name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  bundle:
    name: bundle install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version-file: .ruby-version
          bundler-cache: true
