---
phase: 01-repo-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: [REPO-02]
must_haves:
  truths:
    - "Ruby 3.4.x is installed and active on the developer machine"
    - "`bundle install` (in subsequent waves) will resolve against Ruby 3.4.9, not system Ruby 2.6.10 or brew ruby@3.3"
  artifacts: []
  key_links:
    - from: "developer shell"
      to: "Ruby 3.4.9 interpreter"
      via: "version manager (rbenv/asdf/mise/chruby) reading .ruby-version"
      pattern: "ruby --version output contains '3.4.'"
---

<objective>
Block Phase 1 execution until Ruby 3.4.x (target: 3.4.9) is installed and active on the developer machine. Per RESEARCH §"Environment Availability", the machine currently only has system Ruby 2.6.10 and brew ruby@3.3 — neither satisfies the project pin in `.ruby-version`. Without this checkpoint, the Wave 2 `bundle install` task will fail with a Ruby version mismatch (Pitfall 4 in RESEARCH.md).

Purpose: Prevent a cascading failure later in the phase by surfacing the environmental prerequisite up front. Five-second human verification, no automation in-band (installing a version manager requires user choice and potentially a system password — not something the executor agent should attempt).

Output: A confirmed Ruby 3.4.x runtime on PATH, validated by a single shell command before any file-creation work begins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repo-foundation/01-CONTEXT.md
@.planning/phases/01-repo-foundation/01-RESEARCH.md
@.planning/phases/01-repo-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm Ruby 3.4.x is installed and active</name>
  <read_first>
    .planning/phases/01-repo-foundation/01-RESEARCH.md (§"Environment Availability" and §"Pitfall 4")
    .planning/phases/01-repo-foundation/01-CONTEXT.md (D-14)
  </read_first>
  <what-built>
    Nothing yet — this is a precondition checkpoint. The executor has NOT created any files. The next wave's `bundle install` step requires Ruby 3.4.x, and the machine currently only has Ruby 2.6.10 (system) and Ruby 3.3 (brew). Per D-14 (refined by RESEARCH §1, the target is 3.4.9, not the 3.4.8 written in CONTEXT.md — CLAUDE.md authorizes "latest stable patch").
  </what-built>
  <how-to-verify>
    1. Open a fresh terminal in this working directory: `/Users/adrian.towery/Projects/mutation-testing-example`
    2. Run: `ruby --version`
    3. Expected output: `ruby 3.4.x ...` (where x is 8, 9, or any higher patch). 3.4.9 is the target; 3.4.8 is acceptable; any 3.4.x patch satisfies REPO-02.
    4. If output shows `ruby 2.6.10` or `ruby 3.3.x` or `ruby not found`:
       - Install a Ruby version manager if you don't have one. Recommended: `mise` via `brew install mise` (fastest). Alternatives: `rbenv`, `asdf`, `chruby`.
       - Install Ruby 3.4.9: `mise install ruby@3.4.9` (or your manager's equivalent).
       - Activate locally: `mise use ruby@3.4.9` (creates/updates `.tool-versions` — DO NOT commit `.tool-versions` for this phase; the `.ruby-version` file added in Wave 1 is sufficient).
       - Re-run `ruby --version` and confirm 3.4.x.
    5. Also verify Bundler is available: `bundle --version` should print Bundler 2.x (any 2.x is fine; ships with Ruby 3.4.9).

    Reply "approved" only after `ruby --version` outputs `ruby 3.4.x` AND `bundle --version` outputs `Bundler version 2.x.y`.
  </how-to-verify>
  <verify>
    <human-check>User confirms `ruby --version` shows 3.4.x and `bundle --version` shows 2.x</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms `ruby --version | grep -E '^ruby 3\.4\.'` exits 0
    - Human confirms `bundle --version | grep -E '^Bundler version 2\.'` exits 0
    - User types "approved" or equivalent to resume the wave
  </acceptance_criteria>
  <done>
    Ruby 3.4.x is confirmed active on the developer's PATH and Bundler 2.x is available. The next wave's `bundle install` step is unblocked.
  </done>
  <resume-signal>Type "approved" once `ruby --version` shows 3.4.x. Reply with the actual version string for the SUMMARY record.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine ↔ ruby-builder binary feed | A version manager (mise/rbenv/asdf/chruby) downloads a prebuilt Ruby binary — trusted by virtue of using a well-known version manager and the canonical ruby-build definitions. No new threat surface introduced by this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-PRE-01 | Tampering | Ruby installation source | accept | Standard rbenv/asdf/mise sources are well-known; no new attack surface introduced by Phase 1's checkpoint. User is responsible for using a reputable version manager. |
</threat_model>

<verification>
- `ruby --version` returns a string starting with `ruby 3.4.`
- `bundle --version` returns a string starting with `Bundler version 2.`
- Human types "approved" to acknowledge.
</verification>

<success_criteria>
The phase is unblocked: Ruby 3.4.x is active and Bundler 2.x is available. Wave 1 can proceed.
</success_criteria>

<output>
Create `.planning/phases/01-repo-foundation/01-01-SUMMARY.md` recording the exact Ruby version string the user reported (e.g., "ruby 3.4.9 (2026-03-11 revision …) +YJIT [arm64-darwin25]") and the Bundler version. This lets later phases reference the exact pinned interpreter.
</output>
