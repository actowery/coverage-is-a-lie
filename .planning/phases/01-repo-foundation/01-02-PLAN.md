---
phase: 01-repo-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - LICENSE
  - README.md
  - Gemfile
  - .ruby-version
  - .mutant.yml
  - .gitignore
  - .github/workflows/ci.yml
autonomous: true
requirements: [REPO-01, REPO-02, REPO-03, REPO-04, REPO-05]
must_haves:
  truths:
    - "MIT LICENSE file with 2026 Adrian Towery copyright exists at repo root"
    - "README quickstart shows clone → cd → bundle install plus placeholder commands for future phases"
    - "Gemfile pins exactly rspec ~> 3.13.2, simplecov ~> 0.22.0, mutant ~> 0.16.3, mutant-rspec ~> 0.16.3, and loose rake — all in :development, :test group"
    - ".ruby-version contains exactly 3.4.9"
    - ".mutant.yml contains usage: opensource as a flat YAML key"
    - ".gitignore covers .env*, *.pem, *.key, coverage/, tmp/, .bundle/, vendor/bundle/, *.gem — without excluding Gemfile.lock"
    - "CI workflow uses actions/checkout@v6 and ruby/setup-ruby@v1 with bundler-cache: true and no permissions block"
  artifacts:
    - path: "LICENSE"
      provides: "MIT License text per opensource.org/licenses/MIT"
      contains: "MIT License"
    - path: "README.md"
      provides: "Phase 1 quickstart skeleton"
      contains: "coverage-is-a-lie"
    - path: "Gemfile"
      provides: "Locked dependency declarations"
      contains: 'group :development, :test'
    - path: ".ruby-version"
      provides: "Ruby version pin for rbenv/asdf/mise and ruby/setup-ruby"
      contains: "3.4.9"
    - path: ".mutant.yml"
      provides: "Mutant OSS licensing config"
      contains: "usage: opensource"
    - path: ".gitignore"
      provides: "Ruby build artifact and secret exclusions"
      contains: ".bundle/"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow that validates bundle install"
      contains: "ruby/setup-ruby@v1"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".ruby-version"
      via: "ruby/setup-ruby@v1 with ruby-version-file: .ruby-version (per D-10)"
      pattern: "ruby-version-file:[[:space:]]+\\.ruby-version"
    - from: "Gemfile"
      to: ".ruby-version"
      via: 'ruby file: ".ruby-version" directive (per RESEARCH §"Pattern 1")'
      pattern: 'ruby file: "\\.ruby-version"'
---

<objective>
Create the seven hand-authored Phase 1 source files in parallel: `LICENSE`, `README.md`, `Gemfile`, `.ruby-version`, `.mutant.yml`, `.gitignore`, and `.github/workflows/ci.yml`. Each file is independent — no file depends on another in this wave, so all writes are batch-parallel.

This plan establishes every artifact REPO-01 through REPO-05 requires, EXCEPT `Gemfile.lock` (generated by Wave 2's `bundle install`) and the public-remote/push (Wave 3's `gh repo create`). All contents are locked verbatim by RESEARCH.md §"Code Examples"; no design decisions remain.

Purpose: Land the scaffold's static text artifacts in one parallel wave so Wave 2's `bundle install` and Wave 3's `gh repo create` have a clean working tree to operate on.

Output: Seven files committed to the working tree (uncommitted to git — the next wave handles the commit). The eighth file, `Gemfile.lock`, is produced by Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/01-repo-foundation/01-CONTEXT.md
@.planning/phases/01-repo-foundation/01-RESEARCH.md
@.planning/phases/01-repo-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write LICENSE, README.md, .ruby-version, .mutant.yml</name>
  <files>LICENSE, README.md, .ruby-version, .mutant.yml</files>
  <read_first>
    .planning/phases/01-repo-foundation/01-CONTEXT.md (D-01, D-02, D-14, D-15, D-16)
    .planning/phases/01-repo-foundation/01-RESEARCH.md (§"Code Examples" — LICENSE body, README.md skeleton, .mutant.yml, .ruby-version)
  </read_first>
  <action>
    Write four files at repo root, contents copied verbatim from RESEARCH.md §"Code Examples":

    1. `LICENSE` — Standard MIT body. First line: `MIT License`. Third line: `Copyright (c) 2026 Adrian Towery (Perforce)`. The remaining permission/limitation paragraphs are the unchanged opensource.org/licenses/MIT text. Do not modify the boilerplate (per D-02 and RESEARCH §"Pattern 4" — SPDX MIT identification matters).

    2. `README.md` — Phase 1 quickstart skeleton ONLY. Includes:
       - H1: `# coverage-is-a-lie`
       - One-paragraph description (per RESEARCH §"README.md (Phase 1 skeleton)").
       - `## Quickstart` H2 with a fenced bash block: `git clone https://github.com/actowery/coverage-is-a-lie.git`, `cd coverage-is-a-lie`, `bundle install`.
       - A second fenced bash block with placeholder lines (each preceded by a `#` shell comment indicating the phase that lights them up): `# Phase 2+: bundle exec rspec`, `# Phase 3+: bundle exec mutant run --use rspec --usage opensource`, `# Phase 4+: /llm-mutate --replay      # Claude Code skill, run inside a Claude Code session` (per RESEARCH §"Open Question 4" — the `/llm-mutate` placeholder MUST carry an inline comment so a viewer does NOT paste it into a shell).
       - `## License` H2 with `MIT — see [LICENSE](./LICENSE).`
       - Do NOT write Act 1 / Act 2 narrative content. That is Phase 5's job (per CONTEXT D-16 and Deferred Ideas).

    3. `.ruby-version` — Single line: `3.4.9` followed by a newline. NOT `3.4.8` (CONTEXT D-14 wrote 3.4.8; RESEARCH §1 and §"Research-Surfaced Updates to Locked Decisions" updated this to 3.4.9 — CLAUDE.md authorizes "latest stable patch").

    4. `.mutant.yml` — Exactly one content line: `usage: opensource` (flat scalar, unquoted, no nesting — per D-15 and RESEARCH §"Pitfall 6"). No other keys; Phase 3 extends this file with `integration: rspec` etc.
  </action>
  <verify>
    <automated>
test -f LICENSE && grep -Fxq 'MIT License' LICENSE \
  && grep -Fq 'Copyright (c) 2026 Adrian Towery (Perforce)' LICENSE \
  && test -f README.md && grep -Fq '# coverage-is-a-lie' README.md \
  && grep -Fq 'git clone' README.md \
  && grep -Fq 'bundle install' README.md \
  && grep -Fq '/llm-mutate --replay' README.md \
  && grep -Fq 'Claude Code skill' README.md \
  && test -f .ruby-version && grep -Exq '^3\.4\.9$' .ruby-version \
  && test -f .mutant.yml && grep -Exq '^usage:[[:space:]]+opensource$' .mutant.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` exists; line 1 is exactly `MIT License`; copyright line reads `Copyright (c) 2026 Adrian Towery (Perforce)`
    - `README.md` exists; contains `# coverage-is-a-lie`, `git clone`, `bundle install`, `/llm-mutate --replay`, and an inline `Claude Code skill` annotation
    - `.ruby-version` is exactly the single line `3.4.9` (anchored regex `^3\.4\.9$` matches)
    - `.mutant.yml` contains exactly `usage: opensource` as a flat top-level key (anchored regex `^usage:\s+opensource$` matches)
    - The composite verify command above exits 0
  </acceptance_criteria>
  <done>
    All four scalar text files exist at repo root with their canonical content. The remaining three files in this wave (Gemfile, .gitignore, CI workflow) land in the next two tasks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Gemfile and .gitignore</name>
  <files>Gemfile, .gitignore</files>
  <read_first>
    .planning/phases/01-repo-foundation/01-CONTEXT.md (D-07, D-08, D-09, D-13)
    .planning/phases/01-repo-foundation/01-RESEARCH.md (§"Gemfile" code example, §"Package Legitimacy Audit", §".gitignore" code example, §"Anti-Patterns to Avoid" — do NOT exclude Gemfile.lock)
    CLAUDE.md (§"Recommended Stack" and §"What NOT to Use" — pin versions, no anthropic/claude-ruby/mutest gems)
  </read_first>
  <action>
    Write two files at repo root, contents derived verbatim from RESEARCH.md §"Code Examples":

    1. `Gemfile`:
       ```
       # frozen_string_literal: true

       source "https://rubygems.org"

       # Pin Ruby version (read from .ruby-version for single source of truth)
       ruby file: ".ruby-version"

       group :development, :test do
         gem "rspec",        "~> 3.13.2"
         gem "simplecov",    "~> 0.22.0"
         gem "mutant",       "~> 0.16.3"
         gem "mutant-rspec", "~> 0.16.3"
         gem "rake"
       end
       ```
       Implements D-08 (five gems, all pinned per CLAUDE.md tech stack, all in `:development, :test` group), D-09 (lock file IS committed — handled by Wave 2), and RESEARCH §"Open Question 2" recommendation (include `ruby file:` directive — within Claude's discretion per D-08).

       Per T-1-01 (RESEARCH §"Security Domain"): gem names must be exactly `rspec`, `simplecov`, `mutant`, `mutant-rspec`, `rake` — these are the five names verified live against rubygems.org in RESEARCH §"Package Legitimacy Audit". Do NOT use `mutest`, `claude-ruby`, `anthropic`, or `alexrudall/ruby-anthropic` (CLAUDE.md "What NOT to Use" table).

    2. `.gitignore`:
       ```
       # Source: github.com/github/gitignore/blob/main/Ruby.gitignore (trimmed for demo scope)

       # Bundler / gem builds
       *.gem
       *.rbc
       .bundle/
       vendor/bundle/

       # Test & coverage output (Phases 2-3 generate these)
       /coverage/
       /spec/reports/
       /spec/examples.txt

       # Mutant + LLM mutator output (Phase 3-4 generate these)
       /tmp/

       # OS / editor noise
       .DS_Store
       .byebug_history

       # Documentation cache
       /.yardoc/
       /_yardoc/
       /doc/
       /rdoc/

       # Secrets — never commit (T-1-04 mitigation)
       .env
       .env.*
       *.pem
       *.key

       # Note: Gemfile.lock is INTENTIONALLY committed for this project (it is an
       # application/demo, not a publishable library). Do not add Gemfile.lock here.
       ```
       Implements D-13 (excludes tmp/, .bundle/, vendor/bundle/, coverage/, .DS_Store, *.gem), commits Gemfile.lock per D-09 (the github/gitignore Ruby template's `Gemfile.lock` comment line is intentionally OMITTED here — anti-pattern in RESEARCH §"Anti-Patterns to Avoid"), and mitigates T-1-04 (secrets) by explicitly excluding `.env*`, `*.pem`, `*.key`.
  </action>
  <verify>
    <automated>
test -f Gemfile \
  && grep -Fq 'source "https://rubygems.org"' Gemfile \
  && grep -Fq 'group :development, :test do' Gemfile \
  && grep -Fq 'gem "rspec",        "~> 3.13.2"' Gemfile \
  && grep -Fq 'gem "simplecov",    "~> 0.22.0"' Gemfile \
  && grep -Fq 'gem "mutant",       "~> 0.16.3"' Gemfile \
  && grep -Fq 'gem "mutant-rspec", "~> 0.16.3"' Gemfile \
  && grep -Fq 'gem "rake"' Gemfile \
  && ! grep -Fq 'anthropic' Gemfile \
  && ! grep -Fq 'mutest' Gemfile \
  && test -f .gitignore \
  && grep -Fxq '.bundle/' .gitignore \
  && grep -Fxq 'vendor/bundle/' .gitignore \
  && grep -Fxq '/tmp/' .gitignore \
  && grep -Fxq '/coverage/' .gitignore \
  && grep -Fxq '.DS_Store' .gitignore \
  && grep -Fxq '*.gem' .gitignore \
  && grep -Fxq '.env' .gitignore \
  && grep -Fxq '*.pem' .gitignore \
  && grep -Fxq '*.key' .gitignore \
  && ! grep -Exq '^Gemfile\.lock$' .gitignore
    </automated>
  </verify>
  <acceptance_criteria>
    - `Gemfile` exists with `source "https://rubygems.org"`, `ruby file: ".ruby-version"`, and a single `group :development, :test do … end` block containing exactly the five gems (`rspec`, `simplecov`, `mutant`, `mutant-rspec`, `rake`) with the pessimistic pins from CLAUDE.md
    - `Gemfile` contains no `anthropic`, `claude-ruby`, or `mutest` gem references
    - `.gitignore` contains anchored lines for `.bundle/`, `vendor/bundle/`, `/tmp/`, `/coverage/`, `.DS_Store`, `*.gem`, `.env`, `*.pem`, `*.key`
    - `.gitignore` does NOT contain an uncommented `Gemfile.lock` line (per D-09 / RESEARCH anti-pattern)
    - The composite verify command above exits 0
  </acceptance_criteria>
  <done>
    `Gemfile` and `.gitignore` exist and contain the locked content. The Gemfile is ready for Wave 2's `bundle install` to resolve and lock.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .planning/phases/01-repo-foundation/01-CONTEXT.md (D-10, D-11)
    .planning/phases/01-repo-foundation/01-RESEARCH.md (§"Pattern 2: GitHub Actions workflow", §"Security Domain" — T-1-02 default GITHUB_TOKEN scope, T-1-03 pinned actions)
  </read_first>
  <action>
    Create the directory `.github/workflows/` if it does not exist, then write `.github/workflows/ci.yml`:

    ```yaml
    name: CI

    on:
      push:
        branches: [main]
      pull_request:

    jobs:
      bundle:
        name: bundle install
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v6
          - uses: ruby/setup-ruby@v1
            with:
              ruby-version-file: .ruby-version
              bundler-cache: true
    ```

    Implements D-10 (Ubuntu latest; single Ruby version from `.ruby-version`; only checkout + setup-ruby with `bundler-cache: true`; no `bundle exec rspec` — Phase 2 adds that step). Uses `ruby-version-file: .ruby-version` per CONTEXT D-10 (RESEARCH §"Open Question 3" notes both `ruby-version:` and `ruby-version-file:` work; CONTEXT explicitly calls for `ruby-version-file:`).

    Security:
    - T-1-02 (Elevation of Privilege): NO `permissions:` block at the workflow or job level. The default `GITHUB_TOKEN` scope is read-only for our needs (validate `bundle install`); granting write permissions would expand attack surface unnecessarily.
    - T-1-03 (Tampering): Both actions pinned to their canonical major-version floating tags: `actions/checkout@v6` (current major as of May 2026) and `ruby/setup-ruby@v1` (per the action's own README guidance). No randomly-sourced third-party actions.

    Do NOT add:
    - A `bundle exec rspec` step (Deferred to Phase 2 per CONTEXT Deferred Ideas)
    - A `bundle exec mutant` step (explicitly NOT desired per CONTEXT Deferred Ideas)
    - A CI badge in README (Phase 5 per CONTEXT D-11)
    - A `permissions:` block of any kind (T-1-02)
    - Matrix strategy with multiple Ruby versions (D-10 specifies single Ruby version)
  </action>
  <verify>
    <automated>
test -f .github/workflows/ci.yml \
  && grep -Fq 'name: CI' .github/workflows/ci.yml \
  && grep -Fq 'runs-on: ubuntu-latest' .github/workflows/ci.yml \
  && grep -Fq 'actions/checkout@v6' .github/workflows/ci.yml \
  && grep -Fq 'ruby/setup-ruby@v1' .github/workflows/ci.yml \
  && grep -Fq 'ruby-version-file: .ruby-version' .github/workflows/ci.yml \
  && grep -Fq 'bundler-cache: true' .github/workflows/ci.yml \
  && ! grep -Eq '^[[:space:]]*permissions:' .github/workflows/ci.yml \
  && ! grep -Fq 'bundle exec rspec' .github/workflows/ci.yml \
  && ! grep -Fq 'bundle exec mutant' .github/workflows/ci.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - Contains `name: CI`, triggers on `push` to `main` and on `pull_request`, runs on `ubuntu-latest`
    - Uses `actions/checkout@v6` and `ruby/setup-ruby@v1` (both major-tag floating, per RESEARCH guidance)
    - Includes `ruby-version-file: .ruby-version` and `bundler-cache: true`
    - Contains NO `permissions:` block (T-1-02 mitigation — default GITHUB_TOKEN scope)
    - Contains NO `bundle exec rspec` or `bundle exec mutant` steps (Phase 2/3 territory)
    - The composite verify command above exits 0
  </acceptance_criteria>
  <done>
    CI workflow file exists and is correctly scoped to Phase 1 — it will validate `bundle install` on `push`/`pull_request` once Wave 3 creates the public repo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree ↔ rubygems.org | `Gemfile` declares dependencies that Wave 2's `bundle install` will fetch from rubygems.org. Slopcheck false-positive risk mitigated by rubygems.org cross-verification at research time. |
| GitHub Actions runner ↔ third-party actions | `ci.yml` consumes `actions/checkout@v6` (GitHub-official) and `ruby/setup-ruby@v1` (ruby-org official). No other third-party actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | Gemfile gem names | mitigate | Task 2 writes exactly the four gem names (`rspec`, `simplecov`, `mutant`, `mutant-rspec`) plus `rake`, all verified live against rubygems.org per RESEARCH §"Package Legitimacy Audit". Pessimistic pins `~> 0.16.3` etc. cap unintended major upgrades. Wave 2's `Gemfile.lock` will checksum transitive deps. Verify block ensures no `anthropic`/`mutest` typos slip in. |
| T-1-02 | Elevation of Privilege | `.github/workflows/ci.yml` GITHUB_TOKEN scope | mitigate | Task 3 omits any `permissions:` block. Default GITHUB_TOKEN is read-only for our use case. Verify block asserts `! grep -Eq '^permissions:'` to make the omission auditable. |
| T-1-03 | Tampering | Third-party GitHub Actions | mitigate | Task 3 pins only `actions/checkout@v6` (GitHub-official) and `ruby/setup-ruby@v1` (ruby-org official). No randomly-sourced actions. Verify block asserts both action references are present and the file does not include any others. |
| T-1-04 | Information Disclosure | Accidentally-committed secrets | mitigate | Task 2 writes `.gitignore` lines for `.env`, `.env.*`, `*.pem`, `*.key`. Phase 1 introduces no secrets; the lines are forward-looking protection for later phases. Verify block asserts each line is present. |
| T-1-05 | Tampering | Stale gem dependency | accept | Pessimistic pins (`~> 0.16.3`, `~> 3.13.2`, `~> 0.22.0`) allow patch upgrades via `bundle update`. All five gems are at their latest stable releases as of 2026-05-26 per RESEARCH §"Package Legitimacy Audit". Phase 1 carries no automated upgrade gate — manual `bundle update` post-phase is acceptable for a demo. |
</threat_model>

<verification>
- All seven files exist at the listed paths.
- The three composite verify commands above each exit 0.
- No `Gemfile.lock` yet — that lands in Wave 2 via `bundle install`.
- No git commit yet — Wave 2 stages all files in one commit alongside `Gemfile.lock`.
</verification>

<success_criteria>
Seven static text files in place. REPO-01 (LICENSE), REPO-02 (.ruby-version), REPO-03 (Gemfile pins — lockfile pending Wave 2), REPO-04 (.mutant.yml), and REPO-05 (README quickstart) are all satisfied at the file-content level. Threats T-1-01, T-1-02, T-1-03, T-1-04, T-1-05 each have at least one verify clause asserting mitigation.
</success_criteria>

<output>
Create `.planning/phases/01-repo-foundation/01-02-SUMMARY.md` listing the seven files written, the exact gem pins committed to `Gemfile`, and noting that `Gemfile.lock` and the first git commit are deferred to Wave 2 (plan 03).
</output>
