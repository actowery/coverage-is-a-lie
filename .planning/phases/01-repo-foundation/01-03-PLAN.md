---
phase: 01-repo-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - Gemfile.lock
autonomous: true
requirements: [REPO-03]
must_haves:
  truths:
    - "`bundle install` completes without errors in the working directory"
    - "`Gemfile.lock` is generated and resolves to rspec 3.13.x, simplecov 0.22.x, mutant 0.16.x, mutant-rspec 0.16.x, and rake"
    - "`bundle check` reports the gemset is satisfied"
    - "All eight Phase 1 files are committed together in a single initial commit on the default branch (`main`)"
  artifacts:
    - path: "Gemfile.lock"
      provides: "Locked, checksummed dependency graph"
      contains: "BUNDLED WITH"
  key_links:
    - from: "Gemfile.lock"
      to: "Gemfile"
      via: "bundle install resolution (must stay in sync per Pitfall 1)"
      pattern: "DEPENDENCIES"
    - from: "git HEAD"
      to: "all eight Phase 1 files"
      via: "single initial commit"
      pattern: "Phase 1: initial scaffold"
---

<objective>
Run `bundle install` to resolve the Gemfile written in plan 02 and generate `Gemfile.lock`, then create the initial git commit containing all eight Phase 1 files. This is the only wave in Phase 1 that depends on the Ruby 3.4.x environment confirmed in plan 01 — `bundle install` will fail with a Ruby version mismatch if Ruby 3.4.x is not active (RESEARCH §"Pitfall 4").

Per RESEARCH §"Pitfall 1" and §"Pitfall 3": Gemfile and Gemfile.lock must land in the SAME commit (otherwise CI's deployment-mode `bundler-cache: true` will detect drift and exit 16), and `gh repo create --push` in Wave 3 requires a clean working tree with at least one commit on HEAD.

Purpose: Bridge the static-file scaffold (plan 02) and the public-remote creation (plan 04) by producing the lock file and the first commit in a single atomic step.

Output: `Gemfile.lock` exists at repo root; all eight Phase 1 files are committed to git on a branch named `main`; the working tree is clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repo-foundation/01-CONTEXT.md
@.planning/phases/01-repo-foundation/01-RESEARCH.md
@.planning/phases/01-repo-foundation/01-VALIDATION.md
@.planning/phases/01-repo-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run bundle install and verify Gemfile.lock resolves cleanly</name>
  <files>Gemfile.lock</files>
  <read_first>
    .planning/phases/01-repo-foundation/01-RESEARCH.md (§"Pitfall 1" — deployment-mode drift; §"Pitfall 4" — Ruby version mismatch; §"Code Examples" — Gemfile)
    .planning/phases/01-repo-foundation/01-CONTEXT.md (D-08, D-09)
    Gemfile (just written in plan 02)
  </read_first>
  <action>
    From the repo working directory (`/Users/adrian.towery/Projects/mutation-testing-example`):

    1. Verify Ruby 3.4.x is active (sanity check — plan 01 already gated this, but a second cheap assertion costs nothing):
       `ruby --version | grep -E '^ruby 3\.4\.'`
       If this fails, STOP and surface the failure: plan 01's checkpoint was bypassed somehow.

    2. Run `bundle install` in the working directory. This will:
       - Read `Gemfile` (just written in plan 02).
       - Resolve the five gems and their transitive dependencies against rubygems.org.
       - Generate `Gemfile.lock` with locked versions and `BUNDLED WITH` line indicating the local Bundler 2.x version.
       - This is the first time gems are actually downloaded; the install will populate the local Bundler cache.

    3. Run `bundle check` and assert exit 0. This confirms Gemfile.lock is in sync with Gemfile (catches the Pitfall 1 drift class before it ever reaches CI).

    4. Spot-check the generated lock file:
       - `grep -E '^[[:space:]]+rspec \(3\.13\.' Gemfile.lock` (rspec resolved to 3.13.x)
       - `grep -E '^[[:space:]]+simplecov \(0\.22\.' Gemfile.lock` (simplecov resolved to 0.22.x)
       - `grep -E '^[[:space:]]+mutant \(0\.16\.' Gemfile.lock` (mutant resolved to 0.16.x)
       - `grep -E '^[[:space:]]+mutant-rspec \(0\.16\.' Gemfile.lock` (mutant-rspec resolved to 0.16.x)
       - `grep -E '^[[:space:]]+rake ' Gemfile.lock` (rake resolved to whatever bundles with Ruby 3.4.x)
       - `grep -F 'BUNDLED WITH' Gemfile.lock` (bundler version line present)

    Do NOT run `bundle install --deployment` or set `--frozen`; this is the initial generation, not a redeploy. Do NOT delete and recreate Gemfile.lock if it already exists from a prior partial run — `bundle install` is idempotent and will refresh in place.

    Do NOT add `.bundle/config` to git (`.gitignore` excludes `.bundle/`). Do NOT commit any `vendor/bundle/` directory (also gitignored).
  </action>
  <verify>
    <automated>
ruby --version | grep -Eq '^ruby 3\.4\.' \
  && bundle install --quiet \
  && bundle check \
  && test -f Gemfile.lock \
  && grep -Eq '^[[:space:]]+rspec \(3\.13\.' Gemfile.lock \
  && grep -Eq '^[[:space:]]+simplecov \(0\.22\.' Gemfile.lock \
  && grep -Eq '^[[:space:]]+mutant \(0\.16\.' Gemfile.lock \
  && grep -Eq '^[[:space:]]+mutant-rspec \(0\.16\.' Gemfile.lock \
  && grep -Eq '^[[:space:]]+rake ' Gemfile.lock \
  && grep -Fq 'BUNDLED WITH' Gemfile.lock
    </automated>
  </verify>
  <acceptance_criteria>
    - `ruby --version` returns 3.4.x (sanity check)
    - `bundle install --quiet` exits 0
    - `bundle check` exits 0 ("The Gemfile's dependencies are satisfied")
    - `Gemfile.lock` exists at repo root
    - `Gemfile.lock` lists rspec at 3.13.x, simplecov at 0.22.x, mutant at 0.16.x, mutant-rspec at 0.16.x, rake (any version), and contains `BUNDLED WITH` followed by a 2.x version
    - The composite verify command above exits 0
  </acceptance_criteria>
  <done>
    `Gemfile.lock` is generated and verified in sync with `Gemfile`. REPO-03 ("`bundle install` runs clean") is satisfied at the local level; CI verification follows in plan 04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create initial git commit with all eight Phase 1 files on main</name>
  <files>(none directly modified — this task only stages and commits)</files>
  <read_first>
    .planning/phases/01-repo-foundation/01-RESEARCH.md (§"Pitfall 1", §"Pitfall 3")
    .planning/phases/01-repo-foundation/01-CONTEXT.md (D-09 — lock file IS committed)
    .gitignore (just written in plan 02 — confirms tmp/, .bundle/, vendor/bundle/, coverage/ are excluded)
  </read_first>
  <action>
    Stage all eight Phase 1 files together and commit them in a single atomic commit so Gemfile and Gemfile.lock land at the same SHA (Pitfall 1 mitigation):

    1. Confirm the current branch is `main` (or rename `master` → `main` if the local repo defaults to master):
       - `git symbolic-ref --short HEAD` → if not `main`, run `git branch -M main`.
       - Per RESEARCH §"GitHub Actions workflow" the CI triggers on push to `main`; the branch name MUST be `main`.

    2. Stage exactly the eight Phase 1 files explicitly (do NOT use `git add -A` or `git add .` — per GSD safety protocol, list files by name to avoid accidentally staging `.planning/`, `.claude/`, `.gsd/`, `CLAUDE.md`, or anything else that already lives in the working tree):
       ```
       git add LICENSE README.md Gemfile Gemfile.lock .ruby-version .mutant.yml .gitignore .github/workflows/ci.yml
       ```

    3. Verify the staging set matches expectations BEFORE committing:
       - `git diff --cached --name-only` must list exactly those eight paths (one per line, in some order). The `.planning/` directory, `.claude/`, `.gsd/`, `CLAUDE.md` and any other pre-existing tracked files must NOT appear in the staged set.
       - If the verify shows additional files, unstage them: `git restore --staged <path>`.

    4. Commit with a single concise message:
       ```
       git commit -m "$(cat <<'EOF'
       Phase 1: initial repo scaffold

       LICENSE (MIT), README quickstart, Gemfile pinning rspec/simplecov/mutant/mutant-rspec/rake,
       Gemfile.lock generated by bundle install, .ruby-version pinning 3.4.9, .mutant.yml with
       usage: opensource, .gitignore excluding build/coverage/tmp artifacts and secrets, and a
       minimal CI workflow that validates bundle install on push/PR.

       Implements REPO-01..05.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```

    5. Verify the commit landed and the tree is clean:
       - `git log -1 --pretty=format:'%s'` returns `Phase 1: initial repo scaffold`
       - `git status --porcelain` returns empty (clean working tree — prerequisite for plan 04's `gh repo create --source=. --push`)

    Do NOT amend any pre-existing commits if the repo already has prior history. Always create a NEW commit (GSD safety protocol). Do NOT push to any remote in this task — Wave 3 (plan 04) handles `gh repo create --push`.
  </action>
  <verify>
    <automated>
git symbolic-ref --short HEAD | grep -Fxq 'main' \
  && git log -1 --pretty=format:'%s' | grep -Fxq 'Phase 1: initial repo scaffold' \
  && [ -z "$(git status --porcelain)" ] \
  && git ls-tree -r --name-only HEAD | grep -Fxq 'LICENSE' \
  && git ls-tree -r --name-only HEAD | grep -Fxq 'README.md' \
  && git ls-tree -r --name-only HEAD | grep -Fxq 'Gemfile' \
  && git ls-tree -r --name-only HEAD | grep -Fxq 'Gemfile.lock' \
  && git ls-tree -r --name-only HEAD | grep -Fxq '.ruby-version' \
  && git ls-tree -r --name-only HEAD | grep -Fxq '.mutant.yml' \
  && git ls-tree -r --name-only HEAD | grep -Fxq '.gitignore' \
  && git ls-tree -r --name-only HEAD | grep -Fxq '.github/workflows/ci.yml'
    </automated>
  </verify>
  <acceptance_criteria>
    - Current branch is `main`
    - `git log -1 --pretty=format:'%s'` is exactly `Phase 1: initial repo scaffold`
    - `git status --porcelain` is empty (no uncommitted changes, no untracked files inside the eight tracked paths)
    - All eight Phase 1 files are present in the HEAD tree per `git ls-tree`
    - No remote `origin` exists yet (Wave 3's `gh repo create` adds it)
    - The composite verify command above exits 0
  </acceptance_criteria>
  <done>
    HEAD on `main` contains all eight Phase 1 files in a single commit. The working tree is clean and ready for `gh repo create --source=. --push` in plan 04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Bundler ↔ rubygems.org | `bundle install` downloads gems from rubygems.org and writes their SHA-256 checksums into Gemfile.lock. Gem provenance audit (RESEARCH §"Package Legitimacy Audit") established trust pre-resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 (reaffirmed) | Tampering | Resolved transitive deps in Gemfile.lock | mitigate | `Gemfile.lock` checksums every transitive dep; future `bundle install` will detect tampering. Task 1's verify clause asserts the five top-level gems resolved to their expected versions. |
| T-1-05 (reaffirmed) | Tampering | BUNDLED WITH version mismatch CI ↔ local | mitigate | Wave 1's CI uses `bundler-cache: true` which honors `BUNDLED WITH` from Gemfile.lock — no Bundler-version drift between local and CI. Task 1's verify clause asserts the line is present. |
</threat_model>

<verification>
- Composite verify in Task 1 confirms `Gemfile.lock` resolves correctly.
- Composite verify in Task 2 confirms the commit is on `main`, all eight files are tracked, and the working tree is clean.
- No remote push yet — Wave 3 handles that.
</verification>

<success_criteria>
`Gemfile.lock` exists and is in sync with `Gemfile`. All eight Phase 1 files are at HEAD on `main` in a single commit. Working tree is clean. REPO-03 is fully satisfied at the local level (CI verification of "runs clean from a fresh clone" is plan 04's responsibility).
</success_criteria>

<output>
Create `.planning/phases/01-repo-foundation/01-03-SUMMARY.md` recording the exact resolved gem versions from `Gemfile.lock` (e.g., `rspec 3.13.2`, `mutant 0.16.3`, `mutant-rspec 0.16.3`, `simplecov 0.22.0`, `rake X.Y.Z`), the `BUNDLED WITH` Bundler version, and the commit SHA so plan 04 can reference it.
</output>
