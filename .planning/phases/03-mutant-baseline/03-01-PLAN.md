---
phase: 03-mutant-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/spec_helper.rb
  - Rakefile
  - README.md
autonomous: true
requirements: [MUT-03, MUT-04]

must_haves:
  truths:
    - "Any RSpec example that runs for more than 5 seconds is killed by Timeout::Error, preventing infinite-loop mutations from hanging the mutant run"
    - "SimpleCov does not load when COVERAGE env var is absent (already true; verified by grep pattern)"
    - "rake mutant runs the full mutant command with the correct subject scope and integration flag"
    - "README documents the bundle exec mutant run command so a demo viewer can reproduce the run"
  artifacts:
    - path: "spec/spec_helper.rb"
      provides: "Timeout guard wrapping RSpec.configure around_example"
      contains: "Timeout.timeout(5)"
    - path: "Rakefile"
      provides: "rake mutant task"
      contains: "mutant run"
    - path: "README.md"
      provides: "Act 1 mutant command documentation"
      contains: "mutant-report.txt"
  key_links:
    - from: "spec/spec_helper.rb"
      to: "bundle exec mutant run"
      via: "RSpec.configure around_example block"
      pattern: "Timeout\\.timeout\\(5\\)"
    - from: "Rakefile"
      to: "bundle exec mutant run"
      via: "sh call in rake task"
      pattern: "mutant run"
---

<objective>
Add the Timeout guard to spec_helper (MUT-03), wire a `rake mutant` convenience task to the Rakefile, and document the mutant command in the README — all prerequisites for the actual mutant execution in Plan 02.

Purpose: Prevents infinite-loop mutations from hanging the mutant process indefinitely. Without the timeout guard a single mutant that causes an infinite loop in `add_business_days` (stepping backward through a date range that never converges) would freeze the entire run. This is a real risk given the while-loop in that function.

Output: Three modified files committed to git; the repo is ready to execute `bundle exec mutant run` safely.
</objective>

<execution_context>
@/Users/adrian.towery/Projects/mutation-testing-example/.planning/phases/03-mutant-baseline/03-01-PLAN.md
@/Users/adrian.towery/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adrian.towery/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/adrian.towery/Projects/mutation-testing-example/.planning/PROJECT.md
@/Users/adrian.towery/Projects/mutation-testing-example/.planning/ROADMAP.md
@/Users/adrian.towery/Projects/mutation-testing-example/spec/spec_helper.rb
@/Users/adrian.towery/Projects/mutation-testing-example/Rakefile
@/Users/adrian.towery/Projects/mutation-testing-example/README.md
@/Users/adrian.towery/Projects/mutation-testing-example/.mutant.yml
@/Users/adrian.towery/Projects/mutation-testing-example/lib/date_utils.rb
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Timeout guard to spec_helper.rb and rake mutant task to Rakefile</name>
  <files>spec/spec_helper.rb, Rakefile</files>
  <action>
Modify spec/spec_helper.rb to add a Timeout guard that kills any RSpec example running longer than 5 seconds. This satisfies MUT-03. The guard goes inside an `RSpec.configure` block appended after the existing `require` lines. The block must `require "timeout"` at the top of the file (before the existing `require "date"` line if not present, or at the file top after the frozen_string_literal comment). Use `config.around(:each) do |example| Timeout.timeout(5) { example.run } end` as the around hook body. Do not place the require inside the ENV["COVERAGE"] guard — it must always be loaded regardless of SimpleCov.

The ENV["COVERAGE"] guard in spec_helper already satisfies MUT-04 (SimpleCov does not load during mutant runs). Do not remove or modify the guard.

Modify Rakefile to add a `rake mutant` task. After the existing `task default: :spec` line, add a new task using `desc` and `task` DSL (not RakeTask, just a plain `task :mutant` that calls `sh`). The sh command is: `bundle exec mutant run --integration rspec DateUtils`. Do NOT add `--use rspec` — the `--integration rspec` flag is the correct mutant 0.16.3 flag per the mutant-rspec README. Do NOT add `--include lib` or other flags not present in .mutant.yml defaults. The subject `DateUtils` matches the class name without wildcard (mutant resolves to all methods under the module).

The full rake task:
```
desc "Run mutant mutation testing against DateUtils"
task :mutant do
  sh "bundle exec mutant run --integration rspec DateUtils"
end
```
  </action>
  <verify>
    <automated>ruby -c spec/spec_helper.rb && grep -c "Timeout.timeout" spec/spec_helper.rb && grep -c "mutant run" Rakefile</automated>
  </verify>
  <done>spec/spec_helper.rb parses clean, contains "Timeout.timeout", and Rakefile contains "mutant run"</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document mutant command in README under Act 1 heading</name>
  <files>README.md</files>
  <action>
Read the current README.md to understand its structure. Add a section titled `## Act 1: Traditional Mutation Testing (mutant)` if one does not already exist. This section should appear after the quickstart/installation section and before any Act 2 section. Include:

1. A one-sentence framing: "Even with 100% line and branch coverage, traditional operator-based mutation testing reveals that the test suite cannot catch several boundary bugs."
2. The command to run mutant and capture output: `bundle exec mutant run --integration rspec DateUtils 2>&1 | tee tmp/mutant-report.txt`
3. A note that results are written to `tmp/mutant-report.txt` (gitignored — run locally to reproduce).
4. A note that mutant requires a license acknowledgment: "This repo is open-source. The `.mutant.yml` includes `usage: opensource` which satisfies the license requirement."
5. Expected output shape: kill rate percentage, count of alive mutations, list of surviving mutant diffs.

Do not remove any existing README content. If the README already has an Act 1 section, update it in-place with the above content rather than creating a duplicate.
  </action>
  <verify>
    <automated>grep -c "mutant-report.txt" README.md && grep -c "Act 1" README.md</automated>
  </verify>
  <done>README.md contains "Act 1" heading and "mutant-report.txt" reference; file is valid markdown</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutant subprocess → RSpec examples | mutant forks a subprocess per mutant; the around_example timeout guards against infinite loops in mutated code |
| lib/date_utils.rb mutations → spec harness | Each mutated file is written in-process by mutant; no file system escape beyond tmp/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | add_business_days while-loop | mitigate | Timeout.timeout(5) in RSpec around_example hook kills hanging examples within 5s |
| T-03-02 | Tampering | spec_helper.rb require ordering | accept | SimpleCov ENV guard established in Phase 2 ensures SimpleCov never loads during mutant runs; no new trust exposure |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — all gems already pinned in Gemfile.lock |
</threat_model>

<verification>
- `ruby -c spec/spec_helper.rb` exits 0 (syntax valid)
- `grep "Timeout.timeout(5)" spec/spec_helper.rb` matches
- `grep 'ENV\["COVERAGE"\]' spec/spec_helper.rb` still matches (guard intact)
- `ruby -c Rakefile` exits 0
- `grep "mutant run" Rakefile` matches
- `grep "Act 1" README.md` matches
- `grep "mutant-report.txt" README.md` matches
- `bundle exec rake -T` lists both `spec` and `mutant` tasks
</verification>

<success_criteria>
- spec_helper.rb contains Timeout.timeout(5) around hook (MUT-03 satisfied)
- SimpleCov ENV["COVERAGE"] guard still present and unmodified (MUT-04 confirmed)
- Rakefile has rake mutant task that invokes bundle exec mutant run --integration rspec DateUtils
- README.md has Act 1 section documenting the mutant command and tmp/mutant-report.txt output path
- All three files commit cleanly to git
</success_criteria>

<output>
Create `.planning/phases/03-mutant-baseline/03-01-SUMMARY.md` when done.
</output>
