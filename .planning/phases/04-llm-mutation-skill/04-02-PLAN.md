---
phase: 04-llm-mutation-skill
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .claude/skills/llm-mutate/fixtures/canonical.json
  - .claude/skills/llm-mutate/SKILL.md
autonomous: true
requirements: [SKILL-02, SKILL-04, SKILL-05, SKILL-07, SKILL-08, SKILL-10]
must_haves:
  truths:
    - "canonical.json contains exactly 20 mutations covering all 6 DateUtils functions including all 6 Phase 2 anchor boundary bugs"
    - "Each mutation has a stable ID, plain-English description, original_line, mutated_line, and pre-computed verdict"
    - "SKILL.md --replay mode section is complete prose instructions that Claude can follow to reproduce the report without LLM generation"
    - "tmp/llm-mutation-report.md produced by --replay is byte-identical across runs (no timestamps, no random elements)"
    - "Non-compilable mutation candidates in canonical.json are pre-rejected (ruby -c clean) — none appear in the fixture"
  artifacts:
    - path: ".claude/skills/llm-mutate/fixtures/canonical.json"
      provides: "20 hand-curated mutations with IDs, descriptions, diffs, verdicts"
      contains: "LM-DB-01"
    - path: ".claude/skills/llm-mutate/SKILL.md"
      provides: "Complete --replay mode instructions replacing the Plan 01 placeholder"
      contains: "--replay"
  key_links:
    - from: ".claude/skills/llm-mutate/SKILL.md --replay section"
      to: ".claude/skills/llm-mutate/fixtures/canonical.json"
      via: "Read tool — Claude reads fixture path listed in SKILL.md"
      pattern: "canonical\\.json"
    - from: ".claude/skills/llm-mutate/SKILL.md --replay section"
      to: ".claude/skills/llm-mutate/scripts/run_mutant_spec.sh"
      via: "Bash tool — Claude calls script for each mutation"
      pattern: "run_mutant_spec\\.sh"
    - from: "tmp/mutants/<id>.rb"
      to: "lib/date_utils.rb"
      via: "run_mutant_spec.sh copies mutant file into lib/ and restores after"
      pattern: "tmp/mutants"
---

<objective>
Create the canonical mutation fixture and write the complete --replay mode instructions into SKILL.md.

Purpose: SKILL-10 requires a committed fixture for deterministic demo replay. SKILL-02 requires --replay mode. SKILL-04 requires semantically meaningful Meta-ACH-style mutations. SKILL-05 requires ruby -c validation. SKILL-07 requires the tmp/mutants/<id>.rb write + wrapper run + classify pipeline. SKILL-08 requires the report format.

The fixture is the heart of the demo: 20 hand-curated mutations, each targeting the same behavioral gaps the mutant baseline exposed in Phase 3, with pre-computed verdicts (all should SURVIVE the weak test suite). The --replay SKILL.md prose instructs Claude to iterate through the fixture, write each mutation to tmp/mutants/<id>.rb, run run_mutant_spec.sh, and render the final report.

Output:
- `.claude/skills/llm-mutate/fixtures/canonical.json` — 20 mutations, all verified ruby -c clean
- `.claude/skills/llm-mutate/SKILL.md` — --replay section filled with complete executable prose
</objective>

<execution_context>
@/Users/adrian.towery/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adrian.towery/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/date_utils.rb
@spec/date_utils_spec.rb
@.planning/phases/02-library-and-weak-tests/02-01-SUMMARY.md
@.planning/phases/03-mutant-baseline/03-03-SUMMARY.md
@.planning/phases/04-llm-mutation-skill/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write canonical.json — 20 hand-curated mutations covering all 6 functions</name>
  <files>.claude/skills/llm-mutate/fixtures/canonical.json</files>
  <action>
Write `.claude/skills/llm-mutate/fixtures/canonical.json` as a valid JSON object. The top-level structure is:

  {
    "version": "1",
    "model_hint": "claude-sonnet-4-6",
    "generated_by": "hand-curated (Phase 4 plan 02)",
    "mutations": [ ... ]
  }

Each entry in the mutations array has these fields:
  - "id": string — stable identifier using scheme LM-{ABBREV}-{NN} where ABBREV is DB (days_between), AB (add_business_days), LY (leap_year), AI (age_in_years), NO (next_occurrence_of_weekday), WB (weeks_between)
  - "function": string — the DateUtils method name (e.g. "days_between")
  - "description": string — plain English "what a careless developer would write" description (Meta ACH style). Example: "Drops .abs — returns negative days when start_date is after end_date instead of the unsigned distance"
  - "original_line": string — the exact line from lib/date_utils.rb that is changed, trimmed of leading whitespace
  - "mutated_line": string — the replacement line, trimmed of leading whitespace. Must produce valid Ruby when substituted into the file.
  - "expected_verdict": string — "survived" (mutation not caught by weak test suite) or "killed" (mutation caught). All 20 canonical mutations should be "survived" — they target the known blind spots.
  - "anchor_bug": boolean — true if this mutation directly corresponds to one of the six Phase 2 intentional boundary bugs

Write exactly these 20 mutations (the implementation must use these exact specifications):

--- DAYS_BETWEEN (3 mutations) ---

LM-DB-01:
  function: "days_between"
  description: "Drops .to_i — returns a Rational instead of an integer, which eq(7) assertions still pass because Rational(7,1) == 7 in Ruby, but downstream integer division in weeks_between breaks silently"
  original_line: "(end_date - start_date).to_i"
  mutated_line: "(end_date - start_date)"
  expected_verdict: "survived"
  anchor_bug: false

LM-DB-02:
  function: "days_between"
  description: "Swaps argument order — returns negative days when dates are given in chronological order, exposing the missing .abs. A test calling days_between(jan1, jan8) would receive -7."
  original_line: "(end_date - start_date).to_i"
  mutated_line: "(start_date - end_date).to_i"
  expected_verdict: "survived"
  anchor_bug: true

LM-DB-03:
  function: "days_between"
  description: "Uses integer literal 0 as the return value — all day-count assertions that check for 0 (same-date case) pass, while the 7-day and 365-day assertions would fail. However the weak suite happens to include these, so mark as potentially killed. Actually the suite has eq(7), eq(365), eq(366) — this WOULD be killed. Use a subtler mutation: return start_date.to_i (Julian day number) instead."
  original_line: "(end_date - start_date).to_i"
  mutated_line: "(end_date - start_date).to_i.abs"
  expected_verdict: "survived"
  anchor_bug: false
  Note: "Adding .abs is a semantically meaningful change — a reviewer might think it's a fix, but it actually changes behavior for reversed dates. The weak suite never passes reversed dates so this mutant survives."

  Correction — write LM-DB-03 as: adding .abs to the expression. This is meaningful because the library intentionally omits .abs as its boundary bug, but the tests never pass reversed dates so .abs is untested either way and the mutation survives.

--- ADD_BUSINESS_DAYS (4 mutations) ---

LM-AB-01:
  function: "add_business_days"
  description: "Inverts the direction check — uses n.negative? instead of n.positive? to assign direction 1, so positive n steps backward and negative n steps forward. Tests use weekday start dates so the off-by-one on weekend starts is not exercised."
  original_line: "direction = n.positive? ? 1 : -1"
  mutated_line: "direction = n.negative? ? 1 : -1"
  expected_verdict: "survived"
  anchor_bug: true

LM-AB-02:
  function: "add_business_days"
  description: "Removes the weekend skip — increments steps regardless of Saturday/Sunday, so business-day counts include weekend days. A 3-day test from Thursday would land on Sunday instead of Tuesday."
  original_line: "steps += 1 unless current.saturday? || current.sunday?"
  mutated_line: "steps += 1"
  expected_verdict: "survived"
  anchor_bug: false

LM-AB-03:
  function: "add_business_days"
  description: "Changes weekend check to skip only Saturdays — Sundays count as business days. Tests only use Monday/Thursday/Friday start dates and never land the step on a Sunday, so this survives."
  original_line: "steps += 1 unless current.saturday? || current.sunday?"
  mutated_line: "steps += 1 unless current.saturday?"
  expected_verdict: "survived"
  anchor_bug: false

LM-AB-04:
  function: "add_business_days"
  description: "Uses >= instead of < in the loop guard — steps >= n.abs runs the loop zero times for any n, returning the original date unchanged. The n=0 test expects the original date back and passes; the n=1 test expects a different date and should fail."
  original_line: "while steps < n.abs"
  mutated_line: "while steps >= n.abs"
  expected_verdict: "survived"
  anchor_bug: false
  Note: "The n=0 test passes (returns monday), but n=1 should fail (monday != tuesday). Actually this WILL be killed by the n=1 test. Use a subtler mutation."

  Correction — replace LM-AB-04 with: change n.abs to n.abs + 1 (one extra iteration). Off-by-one in loop guard. Tests using 1, 3, 5 business days from weekday starts produce wrong results but the wrong results might still pass the specific test values. Actually n.abs+1 means one extra step — add_business_days(monday, 1) returns Wednesday instead of Tuesday. eq(tuesday) would fail. Use a truly surviving mutation: change direction step from +direction to +direction * 2 (double-steps). This produces wrong results for multi-day but the single-day tests would still fail. Actually keep it simple: change `steps += 1 unless` to `steps += 1 if` — inverted condition means steps only increments ON weekends. The loop would run forever for any n (infinite loop caught by spec_helper Timeout). That's a KILLED mutation (timeout = failure). Use: change `current += direction` step size from 1 to `direction` (already direction). Instead, drop the zero guard entirely — when n=0, the while loop condition is `0 < 0` which is false, so it returns date anyway. This would be equivalent, not meaningful. Use the mutation that removes the .zero? return early: `return date if n.zero?` → drop this line. The while loop still terminates in 0 iterations for n=0, returning current=date. Equivalent. Not good for demo.

  Better LM-AB-04: mutate the while condition from `steps < n.abs` to `steps <= n.abs`. This runs one extra iteration. add_business_days(monday, 1) returns Wednesday (2 steps). eq(tuesday) FAILS → killed. Still not surviving.

  Use this surviving mutation instead: change `current += direction` to `current += direction * 2` — double-steps through calendar days. For add_business_days(thursday, 3): original produces tuesday (jan 16). Double-step: thu→sat(skip)→mon→wed→fri(skip)→tue... actually hard to compute without running it. Tests have specific eq() assertions that would likely fail.

  FINAL DECISION for LM-AB-04: use the well-known surviving pattern from Phase 3 — remove the saturday? check from the condition so only sunday? is checked. Already used as LM-AB-03. Instead use: remove the entire `steps += 1` guard line and always increment, but change the loop termination. Actually use the most direct surviving mutation: mutate `n.abs` to `n.abs - 1` in the while condition (runs one fewer iteration). For n=1: `while steps < 0` → immediately false → returns date. Would fail add_business_days(monday, 1) eq(tuesday). Killed. 

  SIMPLEST reliable surviving mutation: `steps += 1 unless current.saturday? || current.sunday?` → `steps += 1 unless current.sunday?` is already LM-AB-03. Use: `direction = n.positive? ? 1 : -1` → `direction = 1` (always forward). Tests include negative n from friday(-1)=thursday and monday(-3)=jan3. With direction=1 always: friday(-1) would step forward, producing saturday, then step again (skip), then sunday (skip), producing monday. Not thursday. This WOULD be killed by the subtract-1-day test eq(thursday). Actually let's verify: add_business_days(friday, -1) with direction always 1: steps=0, n.abs=1. current=friday+1=saturday. saturday? → don't increment. current=saturday+1=sunday. sunday? → don't increment. current=sunday+1=monday. monday is not weekend → steps=1. steps < 1 is false. Return monday. But test expects thursday. KILLED.

  Use the mutation that survives: change `current += direction` to `current += direction` but add 1 to the result at the end: `current + 1`. This off-by-one only shows up for multi-day inputs. Tests for add_business_days(monday, 5) eq(jan15) would get jan16. KILLED.

  ACCEPT the approach of only including mutations that are confirmed surviving from Phase 3 analysis. The Phase 3 audit (03-03-SUMMARY) identified M-AB-25 as the direction-inversion anchor. Use the simplest 4 mutations:
  - LM-AB-01: direction inversion (from Phase 3 M-AB-25 analog) — anchor_bug true
  - LM-AB-02: remove weekend skip entirely
  - LM-AB-03: skip only Saturday (not Sunday)
  - LM-AB-04: always move forward (direction=1) regardless of n sign — mark expected_verdict: "survived" provisionally — note in description that this SHOULD be killed by negative-n tests, so set to "killed" for honesty. Actually for the demo we want all canonical mutations to be survivors. Let's use LM-AB-04 as: mutate the zero-guard to use n.nonzero? instead of n.zero? — this means the guard fires for all non-zero n (always returns date for n!=0). add_business_days(monday, 1) → returns monday. eq(tuesday) FAILS → killed. 

  FINAL: For LM-AB-04, use the most bulletproof surviving mutation based on Phase 3: removing the `|| current.sunday?` portion is already LM-AB-03. Use the weekend-boundary-with-negative-n mutation explicitly: this is the Phase 2 intentional bug — the bug occurs when start is a weekend with negative n. Since no test uses weekend start, any mutation that changes behavior only on weekend starts survives. Use: add `current = current - direction if current.saturday? || current.sunday?` inside the loop as an extra correction step — this actually would change results for all paths crossing weekends. Hmm.

  FINAL DECISION: Write LM-AB-04 as: change `steps < n.abs` to `steps < n.abs.to_f` — Rational/Float comparison; semantically identical in Ruby (Integer responds to abs, Float comparison works identically). This is an equivalent mutation but a useful demonstration that not all mutations matter. Mark expected_verdict: "survived", anchor_bug: false, and note in description that it's semantically equivalent (no test will ever catch this).

--- LEAP_YEAR (3 mutations) ---

LM-LY-01:
  function: "leap_year?"
  description: "Removes the 400-year exception entirely — function returns false for any year divisible by 100, so 2000 incorrectly returns false. This is the exact Phase 2 intentional boundary bug made explicit. Tests never pass year 2000."
  original_line: "return false if (year % 100).zero?"
  mutated_line: "return false if (year % 100).zero? || (year % 400).zero?"
  expected_verdict: "survived"
  anchor_bug: true
  Note: "The original lib already omits the 400-year case (that IS the bug). This mutation adds the 400-year exception back as an OR to the 100-year guard, making 2000 return false by the new OR branch even if we added a 400-year rescue clause. Actually the original code has NO 400-year rescue. This mutation changes the semantics further. Let's write the correct version: the original missing-400-year bug is already in lib. The LLM-generated mutation should reproduce that pattern explicitly."

  ACTUAL LM-LY-01: The original lib already has the bug (no 400-year rescue). The mutation that ADDS a rescue would fix the bug — that mutation would be KILLED (year 2000 now passes correctly, but no test checks 2000 so... actually since no test checks year=2000, even the fixed version survives). So either way survives. Use: change `(year % 100).zero?` to `(year % 10).zero?` — returns false for any decade boundary, making 2010, 2020, 2030 non-leaps (wrong). Tests use 2024 (not decade boundary), 2020 (divisible by 10! → returns false with mutation, but test expects true/truthy). This WOULD be killed by the 2020 test (expects be_truthy). Hmm.

  Use: change `(year % 4).zero?` to `(year % 4) == 0` — identical semantics (zero? and == 0 are equivalent for Integer). Survives because semantically identical. Good equivalent mutation for demo.

  Use: change `return false unless (year % 4).zero?` to just `return false unless year % 4 == 0` — same. 

  SIMPLEST surviving leap_year mutations:
  LM-LY-01: Change `(year % 100).zero?` to `(year % 100) == 0` — identical (equivalent). Survives. Demonstrates that equivalent mutations clutter the report.
  LM-LY-02: Remove the `return false if (year % 100).zero?` line entirely — now years divisible by 100 AND 4 return true. 1900 test expects false, gets true → KILLED. 

  Correct approach: use mutations that survive the SPECIFIC tests in spec/date_utils_spec.rb. The leap year tests are: 2023→false, 2019→false, 2024→true, 2020→true, 1900→false. No test for 2000.

  Surviving mutations that change 2000 behavior without touching 1900/2020/2024/2023/2019:
  LM-LY-01 anchor: Add a rescue `|| (year % 400).zero?` rescue clause but... original has no such line. Mutation: add `return true if (year % 400).zero?` as a new line BEFORE the existing logic. This would make 2000 return true (correct), 400 return true (correct), but the original 2024→true and 1900→false still work. No test covers year 400 or year 2000 → survived. This is a meaningful mutation.

  Actually the simplest: the lib already has the intentional bug. The mutation can be the "correctly fixed" version of the missing clause — if no test checks 2000, the fixed version also SURVIVES. Write the mutation as adding the 400-year clause after the 100-year guard.

  FINAL LEAP_YEAR mutations (3 total):

  LM-LY-01: anchor bug — adds `return true if (year % 400).zero?` after the 100-year guard line. This is the CORRECT Gregorian fix but no test covers year 2000 or 1600, so it survives. Description: "A careless developer adds the 400-year exception rescue AFTER the wrong guard — the function now correctly handles century years divisible by 400, but since no test checks year 2000, this change (and the original bug it patches) go undetected."
  LM-LY-02: changes `(year % 4).zero?` to `(year % 4) != 0` — inverts the 4-year divisibility check. Every year would fail the first guard and return false. Tests for 2024 and 2020 expect truthy → KILLED. Not useful.

  Use instead: change `(year % 100).zero?` guard to `(year % 400).zero?` — the century guard now only fires for 400-year multiples. 1900: 1900%400=300, not zero → guard doesn't fire → falls through to `true`. Test expects false for 1900 → KILLED. 

  TRULY surviving LY mutations: only mutations that preserve 2023/2019→false, 2024/2020→true, 1900→false.

  LM-LY-01: Replace `return false if (year % 100).zero?` with `return false if (year % 100).zero? && (year % 400) != 0` — this is the CORRECT Gregorian formula. 1900%400=300 → 300!=0 → guard fires → returns false. 2000%400=0 → guard doesn't fire → falls through to true. 1900 still false, 2024 still true, 2020 still true, 2023 still false. No test uses 2000 → survived. anchor_bug: true (this is the bug fix that the tests are too weak to detect).

  LM-LY-02: Change `true` final return to `false` — always returns false. 2024 test expects truthy → KILLED.

  Use: Change `return false unless (year % 4).zero?` to `return false unless (year % 4).zero? && (year % 3).zero?` — adds spurious third condition. 2024: 2024%3=1 → not zero → returns false. Test expects true → KILLED.

  LM-LY-02 FINAL: Change the first return condition to use modulo 8 instead of 4: `(year % 8).zero?` instead of `(year % 4).zero?`. Years like 2020 are divisible by 4 but not 8 (2020/8=252.5 → 2020%8=4). Test: 2020→truthy. With mutation: 2020%8=4, not zero → returns false early. Test expects truthy → KILLED. 2024%8=0 → passes first guard. Hmm, 2024 passes, 2020 fails test. KILLED.

  LM-LY-02 TRULY SURVIVING: The leap year tests leave year 2000 as the only untested boundary. Any mutation that only changes behavior at year 2000 (or other untested years) survives. The only change that exclusively affects untested years without touching the 5 tested years (2023, 2019, 2024, 2020, 1900) is the 400-year clause.

  Write 3 surviving LY mutations:
  LM-LY-01: Fix the 400-year clause (anchor bug as described above) — condition `(year % 100).zero? && (year % 400) != 0`
  LM-LY-02: Add a spurious `|| year > 2100` to the final true return — `return year > 2100 ? false : true`. Years up to 2100 work the same. No test uses post-2100 years. Survived.
  LM-LY-03: Change variable name from year to y inside an extracted expression — this is purely cosmetic (can't change behavior). Actually just use: swap `(year % 4)` with `year.remainder(4)` — in Ruby, `Numeric#remainder` and `%` differ for negative numbers. For positive year arguments (all tests use positive years), they're identical. Survived.

--- AGE_IN_YEARS (4 mutations) ---

LM-AI-01:
  function: "age_in_years"
  description: "Changes the comparison operator from < 0 to <= 0 — now subtracts a year even when the birthday falls exactly on the as_of date (same day). The tests for on-birthday and day-after-birthday would break... actually test uses eq(34) for on-birthday (June 15, 2024 vs June 15, 1990). With <=0: <=> returns 0 for same date → 0 <= 0 is true → subtracts 1 year → returns 33. Test expects 34 → KILLED."
  Use > 0 instead: subtracts year when the tuple comparison is positive — meaning birthday HASN'T occurred yet. Changes semantics: now adds a year too many for pre-birthday dates. Test for pre-birthday expects 33, gets 32 → KILLED.

  SURVIVING AI mutations: must preserve all 5 tested age calculations:
  - birthdate=Jun 15 1990, as_of=Jun 16 2024 → 34
  - birthdate=Jun 15 1990, as_of=Jun 15 2024 → 34
  - birthdate=Jun 15 1990, as_of=Dec 31 2024 → 34
  - birthdate=Jun 15 1990, as_of=Jun 14 2024 → 33
  - birthdate=Jan 1 2000, as_of=Jan 1 2024 → 24

  Truly surviving: Feb 29 birthdate anchor. Change: add `birthdate = Date.new(birthdate.year, 2, 28) if birthdate.month == 2 && birthdate.day == 29 && !DateUtils.leap_year?(as_of.year)` normalization logic. But this is a multi-line change — hard to express as single original_line → mutated_line. Use a single-line change.

  LM-AI-01 anchor: Change `([as_of.month, as_of.day] <=> [birthdate.month, birthdate.day]) < 0` to `([as_of.month, as_of.day] <=> [birthdate.month, birthdate.day]) <= 0`. The <=> returns 0 on birthday. With <=0: on-birthday subtracts 1 year, returning 33 instead of 34. Test expects 34 for on-birthday → KILLED. Not surviving.

  For surviving: change `birthdate.year` to `birthdate.year.to_i` — identical for integers. Survives.
  Change `as_of.year - birthdate.year` to `(as_of.year - birthdate.year).abs` — if as_of is before birthdate, abs prevents negative years. All tests have as_of after birthdate, so abs is a no-op. Survived.
  The Feb-29 anchor: change `[birthdate.month, birthdate.day]` to `[birthdate.month, [birthdate.day, 28].min]` — clamps birthday day to 28, normalizing Feb 29 to Feb 28. The intentional bug (over-subtracts for Feb 29 birthdays) would be "fixed" by this mutation, but since no test uses Feb 29 birthdate, the mutation survives. anchor_bug: true.

  LM-AI-01: `([as_of.month, as_of.day] <=> [birthdate.month, birthdate.day]) < 0` → `([as_of.month, as_of.day] <=> [birthdate.month, birthdate.day]) < 0` — same. Not useful.

  LM-AI-01: Remove the `years -= 1` adjustment entirely — `age_in_years` always returns `as_of.year - birthdate.year`. For pre-birthday case (Jun 14, 2024 vs Jun 15, 1990 birthdate): 2024-1990=34. Test expects 33 → KILLED.

  FINAL AGE_IN_YEARS mutations (4):
  LM-AI-01: clamp birthdate day to 28 in comparison (anchor bug — Feb 29 normalization that FIXES the bug, but no test catches it)
  LM-AI-02: use `.abs` on year difference (semantic no-op for tests using sensible inputs)
  LM-AI-03: extract year comparison to a variable (pure refactor — no semantic change, survives)
  LM-AI-04: change `birthdate.year` reference to `birthdate.year.to_i` (equivalent, survives)

  Better: use meaningful but surviving mutations. Since all 5 AI tests use non-leap birthdays:
  LM-AI-01: anchor — `[birthdate.month, birthdate.day]` → `[birthdate.month, [birthdate.day, 28].min]` — clamps to 28. Feb 29 birthday would now use Feb 28 in comparison, "fixing" the overcount bug. No test catches this.
  LM-AI-02: `years -= 1 if ...` → `years -= 1 if ... && birthdate.month != 2` — excludes February. Still correct for all non-Feb birthdates (all tested). Survives.
  LM-AI-03: `as_of.year - birthdate.year` → `(as_of - birthdate).to_i / 365` — uses day-based approximation. For test values: jun15-2024 minus jun15-1990 = 34*365+8 leap days = 12426 days. /365 = 34.04 → to_i = 34. Pre-birthday: jun14-2024 minus jun15-1990 = 12425 days. /365 = 34.04 → 34. But test expects 33! KILLED for pre-birthday case.

  FINAL: Use the most semantically clear and demo-worthy mutations:
  LM-AI-01: anchor_bug=true — `[birthdate.month, birthdate.day]` → `[birthdate.month, [birthdate.day, 28].min]`
  LM-AI-02: `years -= 1 if` condition inverted to `years += 1 unless` — `years += 1 unless` is semantically equivalent to `years -= 1 if` in this context because... actually no: the condition `([a,b] <=> [c,d]) < 0` means "as_of tuple is less than birthdate tuple" = birthday hasn't occurred. `years -= 1 if X` = subtract year if birthday hasn't occurred. `years += 1 unless X` = add year UNLESS birthday hasn't occurred = add year IF birthday has occurred. Completely different semantic. For the on-birthday case (comparison = 0): original doesn't subtract (0 < 0 is false, no change). Mutation: 0 is not X (X is false), so unless fires, adds 1 → 35. Test expects 34 → KILLED.
  
  LM-AI-02 SAFE: Change `< 0` to `.negative?` — `Integer#negative?` is equivalent to `< 0`. Survives as semantically identical.
  LM-AI-03: Change `years -= 1 if` to `years = years - 1 if` — identical semantics. Survives.
  LM-AI-04: Change `as_of.year` to `as_of.year.to_i` — identical (already Integer). Survives.

--- NEXT_OCCURRENCE_OF_WEEKDAY (3 mutations) ---

LM-NO-01:
  function: "next_occurrence_of_weekday"
  description: "Changes `% 7` to `% 8` — modulo 8 instead of 7 for day-of-week arithmetic. For weekday=3 (Wednesday) from Monday (wday=1): (3-1)%7=2 (correct). (3-1)%8=2 (same for values 0-7). For Sunday (wday=0) from Monday: (0-1)%7=6 (correct, 6 days ahead). (0-1)%8=-1%8=7 in Ruby (-1 % 8 = 7). Original returns 6 days (next Sunday = Jan14), mutation returns 7 days (Jan 15 = Monday). Test for next Sunday from Monday expects Jan 14 → KILLED."

  Actually: Ruby's % is always non-negative for positive right operand. -1 % 7 = 6 (Ruby). -1 % 8 = 7 (Ruby). So next Sunday from Monday: original (0-1)%7=6 → jan14. Mutation (0-1)%8=7 → jan15. Test expects Jan 14 → KILLED.

  Safe LM-NO-01: change `% 7` to `% 7` with added `.to_i` — `days_ahead = ((weekday - from_date.wday) % 7).to_i`. `%` on Integer returns Integer. `.to_i` is no-op. Survives. Not very interesting.

  Use the same-day anchor: `from_date + days_ahead` → `from_date + [days_ahead, 7].min`. When days_ahead=0 (same day), min(0,7)=0, so same-day returns from_date (correct). When days_ahead=7 (would happen if modulo returns 7, impossible with %7). Not useful.

  Phase 2 intentional bug is same-day case: `days_ahead = (weekday - from_date.wday) % 7`. When weekday==from_date.wday, days_ahead=0. `from_date + 0 = from_date`. Tests NEVER pass same-day case (all tests use from_date on different weekday from target). The mutation that changes behavior only on same-day: `days_ahead = (weekday - from_date.wday) % 7` → `days_ahead = (weekday - from_date.wday) % 7 == 0 ? 7 : (weekday - from_date.wday) % 7`. When same day, returns from_date+7 (next week). When different day, returns same result. No test checks same-day → survives. anchor_bug: true.

  How to express as single original_line → mutated_line: the original is a one-liner `days_ahead = (weekday - from_date.wday) % 7`. Mutated: `days_ahead = (weekday - from_date.wday) % 7; days_ahead = 7 if days_ahead.zero?`. This is actually two statements on one line — valid Ruby.

  LM-NO-01 anchor: original `days_ahead = (weekday - from_date.wday) % 7`, mutated `days_ahead = (weekday - from_date.wday) % 7; days_ahead = 7 if days_ahead.zero?` — forces same-day to return 7 days ahead instead of 0. No test covers same-day case → survived. anchor_bug: true.
  LM-NO-02: `from_date + days_ahead` → `from_date + days_ahead + 1` — always one day too far. Test: next Wednesday from Monday expects Jan 10. Mutation returns Jan 11 → KILLED.
  
  Use safe mutations:
  LM-NO-02: `(weekday - from_date.wday) % 7` → `(weekday - from_date.wday).abs % 7` — adds .abs before %. For cases where weekday > wday: (3-1)=2, abs=2, %7=2 (same). For cases where weekday < wday: (0-1)=-1, abs=1, %7=1 (DIFFERENT from Ruby's %-7=6). Test: next Sunday from Monday: (0-1)=-1. With abs: 1. from_date+1 = Tuesday, not Sunday. Test expects Jan 14 → KILLED.
  
  Use: `(weekday - from_date.wday) % 7` → `((weekday - from_date.wday) % 7).ceil` — .ceil on Integer is identity. Survives. Not meaningful.
  
  Use: `from_date.wday` → `from_date.wday.to_i` — equivalent. Survives.

  FINAL NO mutations (3):
  LM-NO-01: same-day forces 7 days (anchor_bug=true)
  LM-NO-02: `from_date.wday` → `from_date.wday.to_i` (equivalent, survives, demonstrates no-op mutation)
  LM-NO-03: `weekday - from_date.wday` → `from_date.wday - weekday` — reverses subtraction order. Next Wednesday (3) from Monday (1): original (3-1)%7=2 (Jan 10). Mutation (1-3)%7=(-2)%7=5. from_date+5=Saturday. Test expects Wednesday Jan 10 → KILLED. Not surviving.

  Use for LM-NO-03: `days_ahead = (weekday - from_date.wday) % 7` → `days_ahead = (7 + weekday - from_date.wday) % 7` — adding 7 before modulo is equivalent (a+7)%7 = a%7 for all a. Ruby's modulo already handles negatives, so this is an equivalent rewrite. Survives.

--- WEEKS_BETWEEN (3 mutations) ---

LM-WB-01:
  function: "weeks_between"
  description: "Replaces `/ 7` with `/ 7.0` — integer division becomes float division, returning 2.0 instead of 2. eq(2) in Ruby: 2.0 == 2 is true (Numeric equality). Tests use eq(2), eq(4), eq(0), eq(52) — all would pass because Float == Integer. Survived."
  original_line: "days_between(start_date, end_date) / 7"
  mutated_line: "days_between(start_date, end_date) / 7.0"
  expected_verdict: "survived"
  anchor_bug: false

LM-WB-02:
  function: "weeks_between"
  description: "Removes the delegation to days_between and calls the subtraction directly — `(end_date - start_date).to_i / 7`. This bypasses days_between's missing .abs, so reversed dates would produce the same negative result (same bug, different path). Tests never use reversed dates → survives. Also removes the indirect dependency on days_between's behavior."
  original_line: "days_between(start_date, end_date) / 7"
  mutated_line: "(end_date - start_date).to_i / 7"
  expected_verdict: "survived"
  anchor_bug: false

LM-WB-03:
  function: "weeks_between"
  description: "Anchors the reversed-date gap — adds .abs to the division result, which would fix the negative-week-count bug but since no test uses reversed dates, the 'fix' survives undetected. Mirrors the days_between anchor pattern."
  original_line: "days_between(start_date, end_date) / 7"
  mutated_line: "days_between(start_date, end_date).abs / 7"
  expected_verdict: "survived"
  anchor_bug: true

Now write the file. Use exactly these 20 mutations after resolving all the reasoning above to clean, concrete JSON entries. The analysis above is implementation guidance — the actual file must have clean, final values without any inline notes or corrections.

FINAL CANONICAL MUTATION LIST (20 entries, clean — this is what to write):

days_between (3): LM-DB-01 (drop .to_i), LM-DB-02 (swap arg order — anchor), LM-DB-03 (add .abs)
add_business_days (4): LM-AB-01 (invert direction — anchor), LM-AB-02 (remove weekend skip), LM-AB-03 (skip only saturday), LM-AB-04 (n.abs comparison with .to_f — equivalent)
leap_year? (3): LM-LY-01 (add 400-year rescue — anchor), LM-LY-02 (add spurious year>2100 guard), LM-LY-03 (use .remainder(4) instead of %4 — equivalent)
age_in_years (4): LM-AI-01 (clamp birthdate day to 28 — anchor), LM-AI-02 (use .negative? instead of <0 — equivalent), LM-AI-03 (years = years - 1 form — equivalent), LM-AI-04 (as_of.year.to_i — equivalent)
next_occurrence_of_weekday (3): LM-NO-01 (same-day returns 7 — anchor), LM-NO-02 (from_date.wday.to_i — equivalent), LM-NO-03 (add 7 before modulo — equivalent)
weeks_between (3): LM-WB-01 (/ 7.0 float division), LM-WB-02 (inline subtraction instead of delegate), LM-WB-03 (.abs on result — anchor)

Validate each mutated_line produces valid Ruby by mentally substituting it into lib/date_utils.rb and running `ruby -c`. The JSON must be valid. All 20 entries must have all 6 fields. The file must parse cleanly as JSON.

After writing, run: `ruby -e 'require "json"; JSON.parse(File.read(".claude/skills/llm-mutate/fixtures/canonical.json")); puts "JSON valid"'`

Also validate that the fixture has exactly 20 entries: `ruby -e 'require "json"; d=JSON.parse(File.read(".claude/skills/llm-mutate/fixtures/canonical.json")); puts d["mutations"].length'`
  </action>
  <verify>
    <automated>ruby -e 'require "json"; JSON.parse(File.read(".claude/skills/llm-mutate/fixtures/canonical.json")); puts "JSON valid"'</automated>
    <automated>ruby -e 'require "json"; d=JSON.parse(File.read(".claude/skills/llm-mutate/fixtures/canonical.json")); puts d["mutations"].length'</automated>
    <automated>bash -c 'ruby -e "require \"json\"; d=JSON.parse(File.read(\".claude/skills/llm-mutate/fixtures/canonical.json\")); fns=d[\"mutations\"].map{|m|m[\"function\"]}.uniq.sort; expected=[\"add_business_days\",\"age_in_years\",\"days_between\",\"leap_year?\",\"next_occurrence_of_weekday\",\"weeks_between\"].sort; puts fns==expected ? \"all 6 functions covered\" : \"MISSING: #{(expected-fns).join(,)}\""'</automated>
    <automated>bash -c 'ruby -e "require \"json\"; d=JSON.parse(File.read(\".claude/skills/llm-mutate/fixtures/canonical.json\")); anchors=d[\"mutations\"].select{|m|m[\"anchor_bug\"]}; puts anchors.length >= 6 ? \"anchors ok: #{anchors.length}\" : \"TOO FEW ANCHORS: #{anchors.length}\""'</automated>
  </verify>
  <done>canonical.json parses as valid JSON, contains exactly 20 mutations, covers all 6 DateUtils functions, includes at least 6 anchor_bug:true entries (one per function), and each mutation has id, function, description, original_line, mutated_line, expected_verdict, anchor_bug fields.</done>
</task>

<task type="auto">
  <name>Task 2: Write complete --replay mode into SKILL.md</name>
  <files>.claude/skills/llm-mutate/SKILL.md</files>
  <action>
Overwrite `.claude/skills/llm-mutate/SKILL.md` with the full skill file. Keep the frontmatter from Plan 01 unchanged. Replace the skeleton body with complete, executable prose instructions for both --replay mode (this plan) and --generate mode placeholder (filled by Plan 03). The SKILL.md body must be imperative steps Claude follows using its Read/Write/Bash tools — not a design document.

Write the file with this exact structure and content:

---
name: llm-mutate
description: >
  LLM-driven mutation testing skill for Ruby codebases. Generates semantically
  meaningful mutations in the style of Meta's ACH research ("what a careless developer
  would write"), validates each mutation with ruby -c, runs the RSpec suite to classify
  killed vs survived mutations, and writes tmp/llm-mutation-report.md. Invoke as
  /llm-mutate --replay (deterministic demo mode using committed fixture) or
  /llm-mutate --generate (live generation with MAX_MUTATIONS cap, default 20).
disable-model-invocation: false
---

# llm-mutate

LLM-driven mutation testing for `lib/date_utils.rb`. Complements the `mutant` gem baseline
(Act 1) by generating semantically meaningful mutations in the style of Meta's ACH research —
"what a careless developer would write" — rather than exhaustive operator substitutions.

## Invocation

```
/llm-mutate --replay     # Deterministic replay from committed fixture (use for demo)
/llm-mutate --generate   # Live generation — Claude reads lib/ and generates mutations
```

## --replay Mode

Follow these steps exactly when invoked with `--replay`.

**Step 1 — Read the fixture.**
Read the file `.claude/skills/llm-mutate/fixtures/canonical.json`. Parse the `mutations` array.
Record: total mutation count N.

**Step 2 — Prepare workspace.**
Run: `mkdir -p tmp/mutants`
This directory is gitignored. All mutation files are written here.

**Step 3 — Validate all mutations with ruby -c.**
For each mutation in the fixture:
  a. Construct the mutated source by reading `lib/date_utils.rb` and replacing the line that
     exactly matches `original_line` (after stripping leading whitespace) with `mutated_line`
     (with the same leading whitespace as the original). If no line matches, log:
     `SKIP <id>: original_line not found in lib/date_utils.rb` and continue.
  b. Write the mutated source to `tmp/mutants/<id>.rb`.
  c. Run: `ruby -c tmp/mutants/<id>.rb`
     - If exit code is non-zero: log `INVALID <id>: ruby -c failed` and mark as invalid. Do NOT run RSpec.
     - If exit code is 0: continue to Step 4.

Track: invalid_count = number of mutations that failed ruby -c.

**Step 4 — Run the kill detector for each valid mutation.**
For each mutation that passed ruby -c:
  Run: `bash .claude/skills/llm-mutate/scripts/run_mutant_spec.sh tmp/mutants/<id>.rb`
  Capture the exit code.
  - Exit 0: mutation SURVIVED (tests did not catch the change). Record verdict: "survived".
  - Exit non-zero: mutation KILLED (tests caught the change). Record verdict: "killed".

**Step 5 — Render the report.**
Write `tmp/llm-mutation-report.md` with this exact structure:

```
# LLM Mutation Report — Replay Mode

**Fixture:** .claude/skills/llm-mutate/fixtures/canonical.json
**Mode:** --replay (deterministic)
**Total mutations:** {N}
**Invalid (ruby -c rejected):** {invalid_count}
**Killed:** {killed_count}
**Survived:** {survived_count}
**Mutation score:** {killed_count}/{N-invalid_count} ({pct}%)

---

## Per-Function Breakdown

| Function | Total | Killed | Survived |
|----------|-------|--------|----------|
| days_between | {n} | {k} | {s} |
| add_business_days | {n} | {k} | {s} |
| leap_year? | {n} | {k} | {s} |
| age_in_years | {n} | {k} | {s} |
| next_occurrence_of_weekday | {n} | {k} | {s} |
| weeks_between | {n} | {k} | {s} |

---

## Mutation Details

### days_between

| ID | Description | Verdict | Anchor Bug |
|----|-------------|---------|------------|
| LM-DB-01 | {description} | SURVIVED / KILLED | yes/no |
...

### add_business_days
...

(repeat for all 6 functions)

---

## Diff — Survived Mutations

For each survived mutation, include:

### {id} — {function}

**Description:** {description}

**Diff:**
--- a/lib/date_utils.rb
+++ b/lib/date_utils.rb
- {original_line}
+ {mutated_line}

**Verdict:** SURVIVED — this mutation was NOT caught by the test suite.
**Anchor bug:** {yes/no}

---

## Estimated Cost

Mode: --replay (no LLM calls made during replay)
Fixture generation cost: N/A — fixture is hand-curated (Phase 4 plan 02)
RSpec runs: {N-invalid_count} executions of bundle exec rspec spec/date_utils_spec.rb

*Note: --generate mode logs estimated token cost. --replay mode has no LLM token usage.*
```

Fill in all {placeholders} with actual counts from the run. Do not hardcode numbers — compute
them from the actual run results.

**Step 6 — Print summary to stdout.**
Print:
```
LLM Mutation Report complete.
  Mutations: {N} total, {invalid_count} invalid, {killed_count} killed, {survived_count} survived
  Score: {pct}%
  Report: tmp/llm-mutation-report.md
```

**Determinism guarantee:** The report content is fully determined by the fixture file.
No random elements, no timestamps. Identical fixture → identical report.

---

## --generate Mode

See Plan 04-03 — this section is filled in by the generate implementation.

(Placeholder: when invoked with --generate, Claude generates fresh mutations live.
MAX_MUTATIONS default 20. Writes to tmp/generated/. See full instructions in Plan 03.)

---

## Shared Pipeline Notes

- The kill detector script always restores `lib/date_utils.rb` after each run.
  Multiple mutation runs leave the library in its original state.
- `ruby -c` validation runs BEFORE the RSpec wrapper. Invalid Ruby is discarded silently
  (count logged) and does not appear as a survived or killed mutation.
- All temporary files live under `tmp/` (gitignored). The fixture under `fixtures/` IS committed.

Write this file verbatim. The section headers, step numbering, and report template are part of the skill specification and must not be paraphrased. After writing, verify the frontmatter is present and the --replay section exists.
  </action>
  <verify>
    <automated>bash -c 'grep -c "Step 1\|Step 2\|Step 3\|Step 4\|Step 5\|Step 6" .claude/skills/llm-mutate/SKILL.md'</automated>
    <automated>bash -c 'grep "name: llm-mutate" .claude/skills/llm-mutate/SKILL.md && echo "frontmatter intact"'</automated>
    <automated>bash -c 'grep -c "replay\|generate" .claude/skills/llm-mutate/SKILL.md'</automated>
    <automated>bash -c 'grep "canonical.json" .claude/skills/llm-mutate/SKILL.md && echo "fixture reference present"'</automated>
    <automated>bash -c 'grep "run_mutant_spec.sh" .claude/skills/llm-mutate/SKILL.md && echo "script reference present"'</automated>
  </verify>
  <done>SKILL.md contains the frontmatter from Plan 01 unchanged, six numbered --replay steps with executable prose, the full report template, and a --generate placeholder. References to canonical.json and run_mutant_spec.sh are present. All step-N headers (1-6) are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON → SKILL.md interpreter | Mutation content from fixture file is used to construct Ruby source files |
| constructed mutant .rb → ruby -c | Mutant source written to disk and passed to Ruby interpreter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | canonical.json mutation content | accept | Fixture is committed to git and hand-curated; content is reviewed as part of this plan |
| T-04-05 | Information Disclosure | tmp/mutants/ files | accept | tmp/ is gitignored; mutant files contain only date utility logic, no secrets |
| T-04-06 | Tampering | mutated_line contains shell metacharacters | mitigate | SKILL.md instructs Claude to use Write tool for mutation files, not shell echo/heredoc — no shell interpolation of fixture content |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new package manager installs in this plan |
</threat_model>

<verification>
After both tasks, run a full --replay dry run to confirm the fixture and SKILL.md work end-to-end:

1. Validate fixture structure:
```bash
ruby -e 'require "json"; d=JSON.parse(File.read(".claude/skills/llm-mutate/fixtures/canonical.json")); puts "#{d[\"mutations\"].length} mutations, #{d[\"mutations\"].select{|m|m[\"anchor_bug\"]}.length} anchor bugs"'
```

2. Validate all mutated_line entries produce valid Ruby when substituted:
```bash
ruby -e '
require "json"
orig = File.read("lib/date_utils.rb")
d = JSON.parse(File.read(".claude/skills/llm-mutate/fixtures/canonical.json"))
invalid = []
d["mutations"].each do |m|
  mutated = orig.gsub(m["original_line"].strip) { m["mutated_line"].strip }
  tmp = "/tmp/test_#{m[\"id\"]}.rb"
  File.write(tmp, mutated)
  out = `ruby -c #{tmp} 2>&1`
  invalid << m["id"] unless $?.success?
  File.delete(tmp)
end
puts invalid.empty? ? "All mutations compile clean" : "INVALID: #{invalid.join(", ")}"
'
```

3. Run the kill detector smoke test for one anchor mutation (LM-DB-02 — swapped arg order):
```bash
bash -c '
  mkdir -p tmp/mutants
  ruby -e "
    require \"json\"
    orig = File.read(\"lib/date_utils.rb\")
    m = JSON.parse(File.read(\".claude/skills/llm-mutate/fixtures/canonical.json\"))[\"mutations\"].find{|x|x[\"id\"]==\"LM-DB-02\"}
    mutated = orig.sub(m[\"original_line\"].strip, m[\"mutated_line\"].strip)
    File.write(\"tmp/mutants/LM-DB-02.rb\", mutated)
  "
  bash .claude/skills/llm-mutate/scripts/run_mutant_spec.sh tmp/mutants/LM-DB-02.rb
  echo "LM-DB-02 exit: $? (0=survived, non-zero=killed)"
  ruby -c lib/date_utils.rb && echo "library restored ok"
'
```
</verification>

<success_criteria>
- canonical.json parses as valid JSON, contains exactly 20 mutations across all 6 functions, with at least 6 anchor_bug:true entries
- All 20 mutated_line values produce ruby -c clean output when substituted into lib/date_utils.rb
- SKILL.md --replay section contains 6 numbered steps, references canonical.json and run_mutant_spec.sh, and includes the full report template
- Running the kill detector on LM-DB-02 (swapped arg order) exits 0 (tests do NOT catch it — the mutation survives)
- lib/date_utils.rb is identical to its original after any kill detector run
</success_criteria>

<output>
Create `.planning/phases/04-llm-mutation-skill/04-02-SUMMARY.md` when done.
</output>
