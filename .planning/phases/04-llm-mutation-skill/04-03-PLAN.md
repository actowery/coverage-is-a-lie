---
phase: 04-llm-mutation-skill
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - .claude/skills/llm-mutate/SKILL.md
autonomous: true
requirements: [SKILL-02, SKILL-04, SKILL-05, SKILL-06, SKILL-07, SKILL-08, SKILL-09]
must_haves:
  truths:
    - "SKILL.md --generate mode section contains complete step-by-step instructions Claude can follow"
    - "MAX_MUTATIONS cap of 20 is enforced in the --generate instructions (Claude stops generating after 20 regardless of functions remaining)"
    - "Cost estimation formula is specified in prose: (input_chars/4 * 3.0/1_000_000) + (output_chars/4 * 15.0/1_000_000) labeled as estimated"
    - "--replay produces byte-identical tmp/llm-mutation-report.md across three consecutive runs"
    - "Non-compilable mutations are logged with a count and skipped — they do not appear as survived"
    - "The report includes at least one mutation per function with plain-English description, diff, and verdict"
  artifacts:
    - path: ".claude/skills/llm-mutate/SKILL.md"
      provides: "Complete --generate section replacing the Plan 02 placeholder"
      contains: "MAX_MUTATIONS"
    - path: "tmp/llm-mutation-report.md"
      provides: "Report written by a --replay run used to verify determinism"
      contains: "Mutation score"
  key_links:
    - from: "SKILL.md --generate section"
      to: "lib/date_utils.rb"
      via: "Read tool — Claude reads library to generate mutations"
      pattern: "lib/date_utils\\.rb"
    - from: "SKILL.md --generate section"
      to: "tmp/generated/"
      via: "Write tool — Claude writes each generated mutation to tmp/generated/<id>.rb"
      pattern: "tmp/generated"
    - from: "SKILL.md cost estimation"
      to: "Sonnet 4.6 pricing"
      via: "Inline constants $3/$15 per MTok in skill prose"
      pattern: "3\\.0/1_000_000\\|15\\.0/1_000_000"
---

<objective>
Complete the SKILL.md by writing the full --generate mode instructions, then verify end-to-end replay determinism.

Purpose: SKILL-02 requires both modes. SKILL-06 requires MAX_MUTATIONS enforcement in --generate. SKILL-09 requires the cost estimation footer. This plan fills the --generate placeholder left by Plan 02 with complete executable prose, then runs --replay three times and confirms byte-identical output (SKILL-10 / ROADMAP success criterion 1).

Output:
- `.claude/skills/llm-mutate/SKILL.md` updated with complete --generate mode section
- `tmp/llm-mutation-report.md` produced and verified identical across three runs
</objective>

<execution_context>
@/Users/adrian.towery/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adrian.towery/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/date_utils.rb
@.planning/phases/04-llm-mutation-skill/04-01-SUMMARY.md
@.planning/phases/04-llm-mutation-skill/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill --generate mode section in SKILL.md</name>
  <files>.claude/skills/llm-mutate/SKILL.md</files>
  <action>
Read the current SKILL.md. Locate the `## --generate Mode` section (currently a placeholder ending with "See Plan 04-03"). Replace the placeholder content with these complete instructions. Do not modify any other section.

Replace the placeholder body under `## --generate Mode` with the following prose (write verbatim):

---

## --generate Mode

Follow these steps exactly when invoked with `--generate`.

**Step 1 — Read the library.**
Read `lib/date_utils.rb` in full. Identify all six public functions:
`days_between`, `add_business_days`, `leap_year?`, `age_in_years`,
`next_occurrence_of_weekday`, `weeks_between`.

For each function, note:
- The branching conditions (if/unless/while/each)
- Any arithmetic operations
- Any comparison operators
- Boundary conditions documented in comments (look for "Intentional bug:" annotations)

**Step 2 — Set the budget.**
MAX_MUTATIONS = 20 (default). Distribute across 6 functions: target 3-4 mutations per function,
stopping globally when the total reaches MAX_MUTATIONS. Do not exceed this cap regardless of
how many functions remain. Log the distribution plan before generating.

**Step 3 — Generate mutations using Meta ACH style.**
For each function (in order: days_between, add_business_days, leap_year?, age_in_years,
next_occurrence_of_weekday, weeks_between), generate mutations up to budget:

  Generate each mutation as follows:
  a. Ask yourself: "What would a careless developer write here?" Focus on:
     - Off-by-one errors (< vs <=, +1 vs -1)
     - Missing edge-case handling (skipping a guard clause)
     - Wrong operator (+ instead of -, / instead of *)
     - Incorrect method (wrong String/Integer method that almost works)
     - Semantically plausible but wrong boundary (% 8 instead of % 7)
  b. Express the mutation as a single-line change: one original_line (exact text from lib/)
     and one mutated_line (the replacement). Multi-line mutations are not supported.
  c. Write a plain-English description in the form: "A careless developer [action] — this causes
     [effect] when [condition], but no test exercises this path so it goes undetected."
  d. Assign a stable ID using scheme LM-{ABBREV}-G{NN} where ABBREV is the function abbreviation
     and NN is a two-digit counter (G prefix means generated, not from fixture).

**Step 4 — Validate with ruby -c.**
For each generated mutation:
  a. Construct the mutated source by reading `lib/date_utils.rb` and replacing the matching
     original_line with mutated_line (same leading whitespace).
  b. Write the mutated source to `tmp/generated/<id>.rb`.
  c. Run: `ruby -c tmp/generated/<id>.rb`
     - Exit non-zero: log `INVALID <id>: rejected by ruby -c`. Increment invalid_count.
       Do NOT run RSpec for this mutation.
     - Exit 0: proceed to Step 5.

Track: invalid_count, generated_count (attempted), valid_count (passed ruby -c).

**Step 5 — Run the kill detector.**
For each mutation that passed ruby -c:
  Run: `bash .claude/skills/llm-mutate/scripts/run_mutant_spec.sh tmp/generated/<id>.rb`
  - Exit 0: mutation SURVIVED.
  - Exit non-zero: mutation KILLED.
  Record verdict.

**Step 6 — Render the report.**
Write `tmp/llm-mutation-report.md` using the same format as --replay mode, with these differences:

  - Header: `# LLM Mutation Report — Generate Mode`
  - Source line: `**Source:** Generated live in current Claude Code session`
  - Mode line: `**Mode:** --generate (live LLM generation)`
  - Sections for only the generated mutations (not fixture mutations)
  - Include the estimated cost footer (see below)

**Step 7 — Estimated cost footer.**
Append this section to `tmp/llm-mutation-report.md`:

```
---

## Estimated Cost

**Model:** claude-sonnet-4-6 (session model)
**Pricing:** $3.00/MTok input, $15.00/MTok output

Estimate method: character count ÷ 4 ≈ tokens (rough approximation).
Actual token usage is not accessible from within a Claude Code skill.

| Item | Approx chars | Approx tokens | Rate | Estimated cost |
|------|-------------|---------------|------|----------------|
| Library read (input) | ~2,200 | ~550 | $3/MTok | ~$0.0000017 |
| Mutation generation prompts (input) | ~{gen_count * 500} | ~{gen_count * 125} | $3/MTok | ~${input_cost:.7f} |
| Generated mutation text (output) | ~{gen_count * 300} | ~{gen_count * 75} | $15/MTok | ~${output_cost:.7f} |
| **Total estimated** | | | | **~${total_cost:.6f}** |

*Labeled "estimated" — actual billing may differ. This is an order-of-magnitude guide.*
*For {gen_count} mutations at claude-sonnet-4-6 rates, expect < $0.01 per run.*
```

Fill in the table using these formulas:
  input_chars = gen_count * 500     (approximate per-mutation prompt size)
  output_chars = gen_count * 300    (approximate per-mutation output size)
  input_tokens = input_chars / 4
  output_tokens = output_chars / 4
  input_cost = (input_tokens / 1_000_000.0) * 3.0
  output_cost = (output_tokens / 1_000_000.0) * 15.0
  total_cost = input_cost + output_cost + 0.0000017  (add the library read cost)

Format all cost values as 7 decimal places (e.g., $0.0000019).

**Step 8 — Print summary.**
Print:
```
LLM Mutation Report complete (--generate mode).
  Generated: {gen_count} mutations attempted, {invalid_count} rejected by ruby -c
  Valid mutations run: {valid_count}
  Killed: {killed_count} | Survived: {survived_count}
  Score: {killed_count}/{valid_count} ({pct}%)
  Estimated cost: ~${total_cost:.6f}
  Report: tmp/llm-mutation-report.md
```

---

After replacing the placeholder, verify the section exists by searching for MAX_MUTATIONS in the file.
  </action>
  <verify>
    <automated>bash -c 'grep "MAX_MUTATIONS" .claude/skills/llm-mutate/SKILL.md && echo "generate section present"'</automated>
    <automated>bash -c 'grep -c "Step [1-8]" .claude/skills/llm-mutate/SKILL.md'</automated>
    <automated>bash -c 'grep "3\.0/1_000_000\|3\.00/MTok\|\$3" .claude/skills/llm-mutate/SKILL.md && echo "cost formula present"'</automated>
    <automated>bash -c 'grep "ruby -c" .claude/skills/llm-mutate/SKILL.md | wc -l'</automated>
    <automated>bash -c 'grep "name: llm-mutate" .claude/skills/llm-mutate/SKILL.md && echo "frontmatter intact"'</automated>
  </verify>
  <done>SKILL.md --generate section has Steps 1-8, MAX_MUTATIONS = 20, ruby -c validation prose, cost estimation table with input_cost and output_cost formulas referencing $3/$15 per MTok. Frontmatter is unchanged (name: llm-mutate, disable-model-invocation: false).</done>
</task>

<task type="auto">
  <name>Task 2: Run --replay three times and verify byte-identical output</name>
  <files>tmp/llm-mutation-report.md</files>
  <action>
Execute the --replay pipeline manually (as the SKILL.md instructs Claude to do) and verify
determinism across three consecutive runs. This task also serves as the acceptance test for all
three plans in this phase.

Execute these steps:

**Step A — Prepare workspace.**
Run: `mkdir -p tmp/mutants`

**Step B — Read fixture.**
Read `.claude/skills/llm-mutate/fixtures/canonical.json`. Parse it to get the mutations array.

**Step C — Validate and run all 20 mutations per the SKILL.md --replay Steps 1-6.**
For each mutation:
  1. Construct mutated source: read `lib/date_utils.rb`, substitute original_line → mutated_line
     (match by stripping leading whitespace from original_line; preserve the indentation from
     the source file when writing the mutated line — copy the leading whitespace from the
     matched source line).
  2. Write to `tmp/mutants/<id>.rb`.
  3. Run `ruby -c tmp/mutants/<id>.rb` — track invalid count.
  4. Run `bash .claude/skills/llm-mutate/scripts/run_mutant_spec.sh tmp/mutants/<id>.rb` — capture exit code.
  5. Record: id, function, description, verdict (survived if exit 0, killed if non-zero),
     anchor_bug, original_line, mutated_line.

**Step D — Write tmp/llm-mutation-report.md.**
Using the report template from SKILL.md --replay Step 5, write the complete report.
Fill all computed fields: N, invalid_count, killed_count, survived_count, mutation score %,
per-function breakdown table, and per-mutation detail rows (for all 20 mutations).
Include the diff section for all survived mutations.
Include the Estimated Cost section at the bottom (use replay mode text — "no LLM calls made").

**CRITICAL for determinism:** The report must NOT contain:
- Any timestamp or date string
- Any random or session-specific identifier
- Any reference to "current run" or "today"
- Any system path that differs across machines (use relative paths only)

**Step E — Verify determinism across three runs.**
After writing the first report, capture a hash:
  `md5_run1=$(md5 -q tmp/llm-mutation-report.md 2>/dev/null || md5sum tmp/llm-mutation-report.md | cut -d' ' -f1)`

Run the entire pipeline (Steps B-D) two more times WITHOUT deleting tmp/mutants/ between runs
(write to same files). Capture hashes for run 2 and run 3.

Compare:
  `if [ "$md5_run1" = "$md5_run2" ] && [ "$md5_run1" = "$md5_run3" ]; then echo "DETERMINISM: PASS"; else echo "DETERMINISM: FAIL"; fi`

If determinism fails, identify which section of the report varies and fix it (most likely a
dynamic timestamp — remove it). Re-run until three consecutive runs produce identical output.

**Step F — Print final summary.**
Print the mutation score (killed/total) and the number of anchor bug survivors. Expected for
the demo: most or all of the 20 canonical mutations should SURVIVE the weak test suite, since
they target the same blind spots that the Phase 3 mutant baseline found.

Expected survived count: 16-20 (the test suite is deliberately weak — anchor mutations should
all survive, equivalent mutations also survive, some mutations targeting genuinely-tested paths
may be killed).

If fewer than 10 mutations survive: investigate — the test suite may be stronger than expected
for specific mutations. Log which mutations were killed and update expected_verdict in the
fixture if needed to ensure the report accurately reflects actual verdicts.
  </action>
  <verify>
    <automated>bash -c 'test -f tmp/llm-mutation-report.md && echo "report exists"'</automated>
    <automated>bash -c 'grep "Mutation score" tmp/llm-mutation-report.md && echo "score present"'</automated>
    <automated>bash -c 'grep -c "SURVIVED\|KILLED" tmp/llm-mutation-report.md'</automated>
    <automated>bash -c '
      md5_1=$(md5 -q tmp/llm-mutation-report.md 2>/dev/null || md5sum tmp/llm-mutation-report.md | cut -d" " -f1)
      md5_2=$(md5 -q tmp/llm-mutation-report.md 2>/dev/null || md5sum tmp/llm-mutation-report.md | cut -d" " -f1)
      md5_3=$(md5 -q tmp/llm-mutation-report.md 2>/dev/null || md5sum tmp/llm-mutation-report.md | cut -d" " -f1)
      [ "$md5_1" = "$md5_2" ] && [ "$md5_1" = "$md5_3" ] && echo "DETERMINISM: PASS" || echo "DETERMINISM: FAIL"
    '</automated>
    <automated>bash -c 'ruby -c lib/date_utils.rb && echo "library intact after all runs"'</automated>
    <automated>bash -c 'grep -c "days_between\|add_business_days\|leap_year\|age_in_years\|next_occurrence\|weeks_between" tmp/llm-mutation-report.md'</automated>
  </verify>
  <done>tmp/llm-mutation-report.md exists, contains "Mutation score", references all 6 functions, contains SURVIVED/KILLED verdicts for mutations, passes determinism check (same md5 across three runs), and lib/date_utils.rb is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md prose → Claude interpreter | Claude follows prose instructions using its tools; no code execution of skill file itself |
| tmp/generated/ | Live-generated mutation files exist only during --generate runs; gitignored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | tmp/llm-mutation-report.md | accept | Report contains only date utility source diffs; no secrets or credentials |
| T-04-08 | Denial of Service | --generate infinite loop risk | mitigate | MAX_MUTATIONS = 20 hard cap enforced in Step 2 prose; spec_helper Timeout(5) already guards RSpec runs |
| T-04-09 | Spoofing | Estimated cost labeled as exact | mitigate | SKILL.md explicitly labels cost section "estimated" and "labeled estimated — actual billing may differ"; footer note added |
| T-04-10 | Tampering | Generated mutations in tmp/generated/ persisting between sessions | accept | tmp/ is gitignored; tmp/generated/ only affects local workspace; no security boundary crossed |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new package manager installs in this plan |
</threat_model>

<verification>
Full phase 4 acceptance test — all five ROADMAP success criteria:

**Criterion 1: --replay produces identical output across three consecutive runs.**
```bash
# Run 1: already done in Task 2. Capture hash.
md5_1=$(md5 -q tmp/llm-mutation-report.md 2>/dev/null || md5sum tmp/llm-mutation-report.md | cut -d' ' -f1)
# Re-execute the --replay pipeline (follow SKILL.md steps manually or invoke /llm-mutate --replay)
# Run 2 hash:
md5_2=$(md5 -q tmp/llm-mutation-report.md 2>/dev/null || md5sum tmp/llm-mutation-report.md | cut -d' ' -f1)
# Run 3 hash:
md5_3=$(md5 -q tmp/llm-mutation-report.md 2>/dev/null || md5sum tmp/llm-mutation-report.md | cut -d' ' -f1)
[ "$md5_1" = "$md5_2" ] && [ "$md5_1" = "$md5_3" ] && echo "CRITERION 1: PASS" || echo "CRITERION 1: FAIL"
```

**Criterion 2: --generate respects MAX_MUTATIONS and logs estimated cost.**
Verify SKILL.md prose contains MAX_MUTATIONS = 20 and cost formula:
```bash
grep "MAX_MUTATIONS = 20" .claude/skills/llm-mutate/SKILL.md && echo "CRITERION 2a: PASS"
grep "input_cost\|output_cost\|total_cost" .claude/skills/llm-mutate/SKILL.md && echo "CRITERION 2b: PASS"
```

**Criterion 3: Report covers all 6 functions with description+diff+verdict.**
```bash
bash -c '
  for fn in "days_between" "add_business_days" "leap_year" "age_in_years" "next_occurrence_of_weekday" "weeks_between"; do
    grep -q "$fn" tmp/llm-mutation-report.md && echo "COVERED: $fn" || echo "MISSING: $fn"
  done
'
```

**Criterion 4: ruby -c rejects non-compilable mutations with logged count.**
Verify SKILL.md prose includes ruby -c validation step and "invalid_count" tracking:
```bash
grep -c "ruby -c\|invalid_count" .claude/skills/llm-mutate/SKILL.md
```
Expected: >= 4 occurrences.

**Criterion 5: Skill is invokable as /llm-mutate.**
```bash
test -f .claude/skills/llm-mutate/SKILL.md && \
grep "name: llm-mutate" .claude/skills/llm-mutate/SKILL.md && \
echo "CRITERION 5: PASS"
```
</verification>

<success_criteria>
- SKILL.md --generate section contains Steps 1-8 with MAX_MUTATIONS = 20 cap and cost estimation formulas
- Cost estimation uses $3.00/MTok input and $15.00/MTok output (Sonnet 4.6 rates), labeled "estimated"
- tmp/llm-mutation-report.md covers all 6 DateUtils functions in the per-function breakdown table
- Three consecutive --replay runs produce byte-identical tmp/llm-mutation-report.md (md5 match)
- lib/date_utils.rb is unchanged after all test runs (trap handler worked correctly)
- SKILL.md frontmatter intact: name: llm-mutate, disable-model-invocation: false
- All 11 SKILL-* requirements addressed across plans 04-01, 04-02, 04-03
</success_criteria>

<output>
Create `.planning/phases/04-llm-mutation-skill/04-03-SUMMARY.md` when done.
</output>
