---
phase: 05-comparison-and-narrative
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - spec/date_utils_spec.rb
  - tmp/mutant-report.txt
  - tmp/llm-mutation-report.md
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03]

must_haves:
  truths:
    - "git checkout demo/weak-tests lands in a self-consistent state with committed mutant and LLM reports"
    - "git checkout demo/fixed-tests lands in a self-consistent state with a strengthened test suite and refreshed reports"
    - "No manual file edits required when switching between branches"
    - "git checkout main after each branch operation returns executor to the development state"
    - "demo/fixed-tests boundary tests target exactly the six omitted boundary cases documented in Phase 2"
  artifacts:
    - path: "tmp/mutant-report.txt"
      provides: "Committed mutant report on demo/weak-tests"
    - path: "tmp/llm-mutation-report.md"
      provides: "Committed LLM report on demo/weak-tests"
    - path: "spec/date_utils_spec.rb"
      provides: "Augmented spec on demo/fixed-tests with boundary tests added"
  key_links:
    - from: "demo/weak-tests"
      to: "tmp/mutant-report.txt"
      via: "committed artifact"
      pattern: "2\\.31%"
    - from: "demo/fixed-tests"
      to: "spec/date_utils_spec.rb"
      via: "boundary tests"
      pattern: "2000|feb.*29|saturday|reversed"
---

<objective>
Create demo/weak-tests and demo/fixed-tests git branches with committed report artifacts.
Each branch must be self-consistent: git checkout alone is sufficient, no manual edits needed.

Purpose: These branches are the demo's physical evidence — the presenter switches between them
live while narrating. demo/weak-tests is "Act 1 before" and demo/fixed-tests is "Act 2 after."
The diff between the two branches IS the punchline of the demo.

Output:
- demo/weak-tests branch: main + committed tmp/mutant-report.txt + committed tmp/llm-mutation-report.md
- demo/fixed-tests branch: demo/weak-tests base + ~10 boundary-case tests in spec/date_utils_spec.rb
  + refreshed tmp/mutant-report.txt + refreshed tmp/llm-mutation-report.md
- Both branches pushed to origin
- Executor returns to main after all branch work completes
</objective>

<execution_context>
@/Users/adrian.towery/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adrian.towery/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-library-and-weak-tests/02-01-SUMMARY.md
@.planning/phases/02-library-and-weak-tests/02-02-SUMMARY.md
@.planning/phases/03-mutant-baseline/03-03-SUMMARY.md
@.planning/phases/04-llm-mutation-skill/04-03-SUMMARY.md
@spec/date_utils_spec.rb
@lib/date_utils.rb
@.claude/skills/llm-mutate/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo/weak-tests branch with committed reports</name>
  <files>tmp/mutant-report.txt, tmp/llm-mutation-report.md</files>
  <action>
These steps must execute in order. Return to main at the end.

STEP 1 — Verify the current reports exist and are non-empty:
```
test -s tmp/mutant-report.txt
test -s tmp/llm-mutation-report.md
```
If either is missing, generate it first:
- For mutant-report.txt: `bundle exec mutant run --integration rspec DateUtils 2>&1 | tee tmp/mutant-report.txt`
- For llm-mutation-report.md: run the /llm-mutate --replay skill in the session (follow SKILL.md
  --replay mode instructions to produce tmp/llm-mutation-report.md from canonical.json)

STEP 2 — Create the branch from main:
`git checkout -b demo/weak-tests`

STEP 3 — Force-add the gitignored report files to this branch. The .gitignore has a `/tmp/`
entry, so a normal `git add tmp/` would skip them. Use:
`git add -f tmp/mutant-report.txt tmp/llm-mutation-report.md`

STEP 4 — Commit the reports:
`git commit -m "demo(weak-tests): commit baseline mutant and LLM-mutate reports"`

STEP 5 — Push:
`git push -u origin demo/weak-tests`

STEP 6 — Return to main IMMEDIATELY:
`git checkout main`

This step is mandatory. The orchestrator resumes on main after this plan completes.

After returning to main, verify: `git branch --show-current` must print `main`.
  </action>
  <verify>
    <automated>
      git branch -r | grep "origin/demo/weak-tests" &amp;&amp;
      git show demo/weak-tests:tmp/mutant-report.txt | grep -c "%" | awk '{exit ($1 &lt; 1)}' &amp;&amp;
      git show demo/weak-tests:tmp/llm-mutation-report.md | grep -c "mutation score" | awk '{exit ($1 &lt; 1)}' &amp;&amp;
      git branch --show-current | grep -q "^main$"
    </automated>
  </verify>
  <done>
demo/weak-tests exists on origin, contains committed tmp/mutant-report.txt (with kill rate %)
and tmp/llm-mutation-report.md (with mutation score), and git branch --show-current == main.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create demo/fixed-tests branch with boundary tests and refreshed reports</name>
  <files>spec/date_utils_spec.rb, tmp/mutant-report.txt, tmp/llm-mutation-report.md</files>
  <action>
These steps must execute in order. Return to main at the end.

STEP 1 — Create demo/fixed-tests from main (NOT from demo/weak-tests — main already has the
same code, and the committed tmp/ files are gitignored on main so no contamination):
`git checkout -b demo/fixed-tests`

STEP 2 — Add boundary-case tests to spec/date_utils_spec.rb. These are the MINIMAL tests
needed to kill the anchor mutations. Add them as new `it` blocks INSIDE the existing describe
blocks — do not restructure the file. Add the following 10 tests:

Inside `describe ".days_between"`:
  it "returns a positive count for reversed date order" do
    expect(DateUtils.days_between(Date.new(2024, 1, 8), Date.new(2024, 1, 1))).to eq(7)
  end

Inside `describe ".add_business_days"`, inside `context "when n is negative"`:
  it "subtracts 1 business day from Saturday (treats Saturday as after Friday)" do
    expect(DateUtils.add_business_days(Date.new(2024, 1, 13), -1)).to eq(Date.new(2024, 1, 12))
  end

Inside `describe ".leap_year?"`, after the existing year-100 context:
  context "when year is divisible by 400" do
    it "returns true for 2000 (400-year Gregorian exception)" do
      expect(DateUtils.leap_year?(2000)).to be_truthy
    end
  end

Inside `describe ".age_in_years"`:
  it "handles a Feb 29 birthday in a non-leap as_of year" do
    leap_birthday = Date.new(2000, 2, 29)
    expect(DateUtils.age_in_years(leap_birthday, as_of: Date.new(2025, 3, 1))).to eq(25)
  end

Inside `describe ".next_occurrence_of_weekday"`:
  it "returns 7 days later when from_date is already on the target weekday" do
    monday = Date.new(2024, 1, 8)
    expect(DateUtils.next_occurrence_of_weekday(monday, 1)).to eq(Date.new(2024, 1, 15))
  end

Inside `describe ".weeks_between"`:
  it "returns a positive count for reversed date order" do
    expect(DateUtils.weeks_between(Date.new(2024, 1, 15), Date.new(2024, 1, 1))).to eq(2)
  end

NOTE: These tests will FAIL on the weak library (lib/date_utils.rb still has intentional bugs).
That is expected and correct for this branch — the fixed-tests branch demonstrates that
"adding boundary tests exposes the bugs AND kills the mutations." The tests will fail when run
on this branch because the lib code has not been fixed. That is the demo's point: after seeing
0/20 mutations survive, you write boundary tests, and the tests fail — proving the mutations
were detecting real behavioral gaps. Do NOT fix lib/date_utils.rb on this branch.

WAIT — re-reading the demo intent: the demo shows that on demo/fixed-tests the tests now KILL
the mutations. For mutations to be killed, the tests must PASS on the original lib (test passes =
mutation killed when test fails on mutated lib). If the boundary tests fail on the original lib,
they would show as "mutation killed" only because the test itself fails before mutation runs...
but that would make the suite red, which breaks the "100% coverage" premise.

CORRECT interpretation: The demo/fixed-tests branch should have BOTH the boundary tests AND
the fixed library code. The punchline is: "here's what the tests look like when written properly
— and now the mutations are killed." This is the correct reading of DEMO-02 ("suite augmented
with boundary-case tests that kills all previously-surviving mutations") and PROJECT.md ("closed
two-act narrative").

So on demo/fixed-tests:
1. Add the 10 boundary tests to spec/date_utils_spec.rb (as above)
2. ALSO fix the 6 intentional bugs in lib/date_utils.rb:
   - days_between: add `.abs` → `(end_date - start_date).to_i.abs`
   - add_business_days: fix weekend-start + negative-n off-by-one. Change the loop initialization
     to advance past any initial weekend before counting: add after `current = date`:
     `current = current.next until current.monday? || current.tuesday? || current.wednesday? || current.thursday? || current.friday?` only when n is negative and date is a weekend. Cleaner: use the Date#wday check in the step-then-check loop. The simplest correct fix: advance `current` past any initial weekend day before the main loop: `current += 1 while current.saturday? || current.sunday?` when n < 0 (insert at top of negative branch).
     Actually the cleanest fix that doesn't break existing tests: at the start of the method, after `current = date`, add one line: `current = date` (already there), then inside the direction assignment block for negative direction, ensure the step advances correctly. The minimal fix: change `current = date` to `current = date.saturday? ? date - 1 : (date.sunday? ? date - 2 : date)` when direction == -1. This is complex; use the simplest provably-correct fix: replace the single loop with separate loops for positive and negative n, each starting from a weekday.
     Simplest correct implementation for the demo: just note that the boundary test `add_business_days(saturday, -1)` should return the previous Friday. The current code starts at saturday, direction=-1, and loops backward counting business days; saturday itself is not a business day so the first step goes to friday (step=1), done. Test: expects friday. Let the executor verify whether the bug actually fires for this test case — if the test passes on the unfixed lib, the bug description in Phase 2 Summary was about a different negative input. Use `Date.new(2024, 1, 13)` (Saturday) with n=-1; the expected return is `Date.new(2024, 1, 12)` (Friday).
   - leap_year?: add `|| (year % 400).zero?` to the 100-year check:
     `return false if (year % 100).zero? && !(year % 400).zero?`
     (full Gregorian rule)
   - age_in_years: clamp day to 28 for Feb 29 birthdays:
     `bday = [birthdate.day, (birthdate.month == 2 ? 28 : birthdate.day)].min`
     Then compare `[as_of.month, as_of.day] <=> [birthdate.month, bday]`
   - next_occurrence_of_weekday: add same-day → next-week behavior:
     After the modulo line, add: `days_ahead = 7 if days_ahead.zero?`
   - weeks_between: delegate through fixed days_between (which now has .abs), so no change needed
     — weeks_between already calls days_between; once days_between is fixed, weeks_between inherits the fix.

STEP 3 — Run the full test suite and confirm it passes:
`bundle exec rspec`
All tests including the new boundary tests must pass (28 + 10 = 38 examples, 0 failures).
If any test fails, diagnose and fix the implementation — do not proceed until the suite is green.

STEP 4 — Run mutant to refresh tmp/mutant-report.txt:
`bundle exec mutant run --integration rspec DateUtils 2>&1 | tee tmp/mutant-report.txt`
The kill rate should be significantly higher than 2.31%. Expect most of the 253 previously-alive
mutations to now be killed by the boundary tests.

STEP 5 — Run llm-mutate --replay to refresh tmp/llm-mutation-report.md:
Execute the /llm-mutate --replay skill (follow SKILL.md --replay mode instructions). On the
fixed suite, most or all of the 20 canonical mutations should now be killed. The score should
increase substantially from 0/20.

STEP 6 — Stage and commit everything on demo/fixed-tests:
`git add spec/date_utils_spec.rb lib/date_utils.rb`
`git add -f tmp/mutant-report.txt tmp/llm-mutation-report.md`
`git commit -m "demo(fixed-tests): add boundary tests + fix library bugs; refresh mutation reports"`

STEP 7 — Push:
`git push -u origin demo/fixed-tests`

STEP 8 — Return to main IMMEDIATELY:
`git checkout main`

Verify: `git branch --show-current` must print `main`.
  </action>
  <verify>
    <automated>
      git branch -r | grep "origin/demo/fixed-tests" &amp;&amp;
      git show demo/fixed-tests:spec/date_utils_spec.rb | grep -c "2000\|Feb 29\|feb.*29\|saturday\|reversed\|same.*weekday\|days_ahead.*7" | awk '{exit ($1 &lt; 3)}' &amp;&amp;
      git show demo/fixed-tests:tmp/mutant-report.txt | grep -c "%" | awk '{exit ($1 &lt; 1)}' &amp;&amp;
      git branch --show-current | grep -q "^main$"
    </automated>
  </verify>
  <done>
demo/fixed-tests exists on origin, spec/date_utils_spec.rb contains boundary tests (year 2000,
Feb 29, same-weekday, reversed dates), tmp/ reports are committed and show improved kill rate,
and git branch --show-current == main.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git branch ops → origin | Branch creation and force-push of gitignored tmp/ files; scoped to demo/* branches only |
| test runner → lib/ | RSpec runs mutated lib code; tmp/ directory is isolated; no network access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | lib/date_utils.rb on demo/fixed-tests | accept | Bug fixes on demo/fixed-tests are intentional and isolated to that branch; main is unaffected |
| T-05-04 | Tampering | tmp/ force-add to git | accept | tmp/ is gitignored on main; force-add only on demo/* branches; committed files are generated reports with no secrets |
| T-05-05 | Denial of Service | mutant run during branch creation | mitigate | mutant already has Timeout.timeout(5) guard in spec_helper from Phase 3 MUT-03; run will complete in bounded time |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs; branch creation and ruby execution only |
</threat_model>

<verification>
After both tasks complete:

```bash
# Both remote branches exist
git branch -r | grep "origin/demo/weak-tests"
git branch -r | grep "origin/demo/fixed-tests"

# demo/weak-tests has reports at the weak baseline score
git show demo/weak-tests:tmp/mutant-report.txt | grep "%"       # 2.31% kill rate
git show demo/weak-tests:tmp/llm-mutation-report.md | grep "0/20"  # 0/20 score

# demo/fixed-tests has boundary tests
git show demo/fixed-tests:spec/date_utils_spec.rb | grep "2000"  # leap year 2000 test

# Executor is on main
git branch --show-current  # must print: main

# demo/weak-tests and demo/fixed-tests are different
git diff demo/weak-tests demo/fixed-tests -- spec/date_utils_spec.rb | head -20
```
</verification>

<success_criteria>
- demo/weak-tests exists on origin with committed report artifacts (DEMO-01)
- demo/fixed-tests exists on origin with augmented spec and refreshed reports (DEMO-02)
- git checkout between branches requires no manual file edits (DEMO-03)
- Executor is on main when this plan completes (orchestrator safety)
</success_criteria>

<output>
Create `.planning/phases/05-comparison-and-narrative/05-02-SUMMARY.md` when done.
</output>
