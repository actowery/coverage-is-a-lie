---
phase: 06-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tmp/llm-mutation-report.md
  - .planning/phases/06-end-to-end-validation/06-01-SUMMARY.md
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

must_haves:
  truths:
    - "All 14 shot-list beats execute without error and produce the documented expected output"
    - "/llm-mutate --replay on demo/weak-tests produces byte-identical tmp/llm-mutation-report.md across three consecutive runs"
    - "A fresh clone into /tmp completes bundle install and rspec cleanly with no machine-state assumptions"
    - "Any drift between shot-list expected output and actual output is fixed in this phase and documented"
  artifacts:
    - path: "tmp/llm-mutation-report.md"
      provides: "Verified deterministic replay output from final run"
    - path: ".planning/phases/06-end-to-end-validation/06-01-SUMMARY.md"
      provides: "Drift log, fixes applied, final pass/fail verdict per beat"
  key_links:
    - from: "docs/shot-list.md"
      to: "demo/weak-tests branch"
      via: "git checkout demo/weak-tests"
      pattern: "Switched to branch 'demo/weak-tests'"
    - from: "docs/shot-list.md"
      to: "tmp/mutant-report.txt"
      via: "cat tmp/mutant-report.txt"
      pattern: "Coverage:.*2\\.31%"
    - from: ".claude/skills/llm-mutate/SKILL.md"
      to: "tmp/llm-mutation-report.md"
      via: "replay pipeline reads canonical.json"
      pattern: "Mutation score: 0/20"
---

<objective>
Walk all 14 shot-list beats verbatim, capture actual output, diff against documented expected
output, fix any drift, then verify determinism and perform a fresh-clone smoke test.

Purpose: Phase 6 is the hard validation gate — the demo does not leave the repo until it runs
cleanly on the recording machine. This plan proves that gate is cleared.

Output: A drift log in SUMMARY.md recording each beat's pass/fail verdict, any fixes applied,
and the three md5 hashes proving deterministic replay. The repo is demo-ready after this plan
completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/shot-list.md
@docs/narration-script.md
@README.md
@.claude/skills/llm-mutate/SKILL.md
@.planning/phases/05-comparison-and-narrative/05-02-SUMMARY.md
@.planning/phases/05-comparison-and-narrative/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk all 14 shot-list beats and fix drift</name>
  <files>docs/shot-list.md</files>
  <action>
Execute each of the 14 beats from docs/shot-list.md in order. For each beat that has a command,
run it and capture stdout. Compare actual output against the expected output literal documented
in the shot list. Record pass or fail. Fix any drift before moving to the next beat.

The exact expected outputs to verify against for each beat are listed below. Start each beat
from the project root `/Users/adrian.towery/Projects/mutation-testing-example`.

**Beat 1 — Setup: Clone and Install**
Branch: main
Commands: `git clone https://github.com/actowery/coverage-is-a-lie.git` then `cd coverage-is-a-lie`
then `bundle install`
Expected anchor: stdout contains `Bundle complete!`
Additional verify: `ls .claude/skills/llm-mutate/` lists `SKILL.md`
Note: clone into a throwaway path; the actual local working repo is used for all subsequent beats
so that Beat 1 proves the clone story without abandoning the local state. Run clone into
`/tmp/beat1-clone-check`, run bundle install there, capture "Bundle complete!" output, then
discard. All remaining beats run in the project root.

**Beat 2 — Check Out the Weak State**
Command: `git checkout demo/weak-tests`
Expected anchor: `Switched to branch 'demo/weak-tests'`
Drift fix if needed: verify branch exists at origin with `git ls-remote --heads origin demo/weak-tests`

**Beat 3 — Show the Green Test Suite**
Command: `COVERAGE=1 bundle exec rspec`
Expected anchors (ALL must be present):
  - `28 examples, 0 failures`
  - `Line Coverage: 100.0%`
  - `Branch Coverage: 100.0%`
Drift fix: if example count differs, check spec/date_utils_spec.rb on this branch

**Beat 4 — Reveal the Kill Rate**
Command: `cat tmp/mutant-report.txt`
Expected anchors (ALL must be present):
  - `Coverage:        2.31%`
  - `253` (alive mutations count visible somewhere in the report)
  - `6` killed (visible in per-subject summaries — grep for `6/259`)
Drift fix: if tmp/mutant-report.txt is missing, it was not committed; check with
`git show demo/weak-tests:tmp/mutant-report.txt | head -20` and re-commit if needed

**Beat 5 — Show a Surviving Mutation Diff**
Command: `grep -A 10 "M-LY-24\|M-LY-26" tmp/mutant-report.txt | head -25`
Expected anchors: output must contain at least one diff block showing a mutation marked alive;
presence of `alive` or `survived` wording near a mutation ID starting with `M-LY`
If that specific grep is empty: also try `grep -A 10 "alive" tmp/mutant-report.txt | head -40`
to confirm at least one alive mutation diff is visible

**Beat 6 — Introduce llm-mutate**
No command — spoken beat. Verify by reading: `cat docs/comparison.md | head -20` produces
output (file exists). Record: pass if file is non-empty.

**Beat 7 — Run llm-mutate in Replay Mode**
Command: `cat tmp/llm-mutation-report.md`
Expected anchors (ALL must be present):
  - `Total mutations: 20`
  - `Survived: 20`
  - `Mutation score: 0/20 (0.0%)`
  - `Mode: --replay (deterministic)` OR `--replay` somewhere in the mode/header line
Drift fix: if tmp/llm-mutation-report.md is missing or has wrong counts, it was not committed
on demo/weak-tests; check with `git show demo/weak-tests:tmp/llm-mutation-report.md | head -20`

**Beat 8 — Show an LLM Mutation Description**
Command: `grep -A 12 "LM-LY-01" tmp/llm-mutation-report.md`
Expected anchors (ALL must be present in output):
  - `LM-LY-01`
  - `leap_year?` (function name present near the ID)
  - `SURVIVED`
  - `Anchor bug` (the "Anchor bug: yes" field)
Drift fix: if LM-LY-01 is missing from the report, check canonical.json fixture for the ID
and whether the SKILL.md replay pipeline is writing the expected report format

**Beat 9 — Review the Comparison Document**
Command: `cat docs/comparison.md`
Expected anchors (ALL must be present):
  - A table row referencing `add_business_days`
  - `LM-AB-01` (the weekend-normalizer divergence)
  - `LM-WB-02` OR `LM-WB-01` (the float division divergence)
  - At least three rows in the divergences section
Drift fix: if docs/comparison.md is missing or has wrong IDs, cross-reference with
docs/shot-list.md Beat 9 narration for the three highlighted divergences

**Beat 10 — The Turn: Check Out Fixed Tests**
Command: `git checkout demo/fixed-tests`
Expected anchor: `Switched to branch 'demo/fixed-tests'`

**Beat 11 — Show the Improved Test Suite**
Command: `COVERAGE=1 bundle exec rspec`
Expected anchors (ALL must be present):
  - `34 examples, 0 failures`
  - `Line Coverage: 100.0%`
Drift fix: if example count is wrong, check spec/date_utils_spec.rb on demo/fixed-tests for
the 6 boundary tests added in Phase 5

**Beat 12 — Re-run mutant**
Command: `cat tmp/mutant-report.txt`
Expected anchor: `Coverage:        1.75%` (note: this is LOWER than 2.31% due to the
module_function quirk — 341 mutations vs 259, 6 kills either way)
If output shows `2.31%` instead: this branch has the wrong report committed; check with
`git show demo/fixed-tests:tmp/mutant-report.txt | head -5`
Drift fix: if wrong report, re-run `bundle exec rake mutant` and `git add -f tmp/mutant-report.txt`
then amend or add a new commit on demo/fixed-tests

**Beat 13 — Re-run LLM-mutate**
Command: `cat tmp/llm-mutation-report.md`
Expected anchors (ALL must be present):
  - `Total mutations: 13`
  - `Killed: 13`
  - `Mutation score: 13/13 (100.0%)`
  - `Mode:` line referencing `--generate`
Note: demo/fixed-tests uses a generate-mode report (not replay-mode); replay would produce
SKIP results because canonical.json targets the original buggy lines
Drift fix: if counts are wrong or mode is wrong, check `git show demo/fixed-tests:tmp/llm-mutation-report.md`

**Beat 14 — Wrap: Return to main**
Command: `git checkout main`
Expected anchor: `Switched to branch 'main'`

After all 14 beats, verify the pre-recording checklist from docs/shot-list.md:
- `git show demo/weak-tests:tmp/mutant-report.txt | grep Coverage` → contains `2.31%`
- `git show demo/weak-tests:tmp/llm-mutation-report.md | grep "Mutation score"` → contains `0/20 (0.0%)`
- `git show demo/fixed-tests:tmp/llm-mutation-report.md | grep "Mutation score"` → contains `13/13 (100.0%)`

Document every beat result in a running table for Task 3's SUMMARY.
  </action>
  <verify>
    <automated>
      git show demo/weak-tests:tmp/mutant-report.txt | grep -c "2\.31" | grep -v '^#' | grep -v '^ *0$' &&
      git show demo/weak-tests:tmp/llm-mutation-report.md | grep "0/20 (0.0%)" &&
      git show demo/fixed-tests:tmp/llm-mutation-report.md | grep "13/13 (100.0%)" &&
      git branch --show-current | grep "^main$"
    </automated>
  </verify>
  <done>
    All 14 beats have been executed and recorded pass/fail. Any drift has been fixed.
    Current branch is main. Pre-recording checklist assertions all pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Determinism check — three consecutive replay runs md5-matched</name>
  <files>tmp/llm-mutation-report.md</files>
  <action>
Verify VAL-03: `/llm-mutate --replay` on demo/weak-tests produces byte-identical output
across three consecutive runs.

Because the skill runs inside a Claude Code session (not as a standalone script), simulate the
replay pipeline manually using the same steps the SKILL.md --replay mode instructs:

Step 1 — Check out demo/weak-tests:
`git checkout demo/weak-tests`

Step 2 — Run the replay pipeline three times in sequence. For each run (run1, run2, run3):
  a. Delete any existing report to ensure a clean write:
     `rm -f tmp/llm-mutation-report.md`
  b. Execute the replay pipeline exactly as SKILL.md specifies:
     - `mkdir -p tmp/mutants`
     - Read `.claude/skills/llm-mutate/fixtures/canonical.json`
     - For each mutation: construct mutated source → write to `tmp/mutants/<id>.rb` →
       `ruby -c tmp/mutants/<id>.rb` → if valid, run
       `bash .claude/skills/llm-mutate/scripts/run_mutant_spec.sh tmp/mutants/<id>.rb`
     - Collect verdicts and write `tmp/llm-mutation-report.md`
  c. Compute the md5 of the resulting report:
     `md5 tmp/llm-mutation-report.md` (macOS) — capture the hash value

Step 3 — Compare the three md5 values. They MUST be identical.

If they differ: the non-determinism source must be identified before declaring VAL-03 satisfied.
Possible sources: timestamp in the report header, RSpec output ordering, file path differences.
Fix the source and re-run until three consecutive runs match.

Step 4 — Return to main: `git checkout main`

Record the three md5 hashes (they should all be the same value) in the SUMMARY.
  </action>
  <verify>
    <automated>
      git checkout demo/weak-tests 2>/dev/null ;
      HASH1=$(rm -f tmp/llm-mutation-report.md &amp;&amp; mkdir -p tmp/mutants &amp;&amp; bash -c 'ruby -e "require \"json\"; m=JSON.parse(File.read(\".claude/skills/llm-mutate/fixtures/canonical.json\"))[\"mutations\"]; puts m.length"') ;
      echo "Fixture readable, mutation count: $HASH1" ;
      git checkout main 2>/dev/null
    </automated>
  </verify>
  <done>
    Three consecutive replay pipeline executions on demo/weak-tests produce tmp/llm-mutation-report.md
    files with identical md5 hashes. Hashes are recorded in SUMMARY.md. Current branch is main.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fresh-clone smoke test and SUMMARY</name>
  <files>.planning/phases/06-end-to-end-validation/06-01-SUMMARY.md</files>
  <action>
Verify the repo runs cleanly from a completely fresh clone with no local state.

Step 1 — Clone into a temporary directory:
`git clone https://github.com/actowery/coverage-is-a-lie /tmp/coverage-fresh`

Step 2 — Install dependencies:
`cd /tmp/coverage-fresh && bundle install`
Expected: `Bundle complete!` (or equivalent Bundler success message with gem count)

Step 3 — Run the test suite:
`cd /tmp/coverage-fresh && bundle exec rspec`
Expected: `28 examples, 0 failures` (on main branch — no coverage env var needed here)

Step 4 — Verify the skill file is present:
`ls /tmp/coverage-fresh/.claude/skills/llm-mutate/SKILL.md`
Expected: file listed without error

Step 5 — Verify both demo branches exist on origin (no local branch needed):
`cd /tmp/coverage-fresh && git ls-remote --heads origin demo/weak-tests demo/fixed-tests`
Expected: two refs listed

Step 6 — Clean up:
`rm -rf /tmp/coverage-fresh`

If any step fails: fix the root cause in the main repo (e.g., a file was not committed,
a branch was not pushed, a gem version conflict exists on clean install). Push fixes before
declaring smoke test passed.

After all tasks complete, write `.planning/phases/06-end-to-end-validation/06-01-SUMMARY.md`
using the project summary template. Include:
- A beat-by-beat results table (Beat | Expected anchor | Actual output | Status)
- The three md5 hashes from Task 2 (all should match)
- Fresh-clone smoke test result
- Any drift fixes applied (file changed, what was wrong, what was fixed)
- Final verdict: DEMO READY or BLOCKED (with blocking issues)
  </action>
  <verify>
    <automated>
      git -C /tmp/coverage-fresh rev-parse HEAD 2>/dev/null || echo "clone succeeded (already cleaned)" ;
      test -f .planning/phases/06-end-to-end-validation/06-01-SUMMARY.md &amp;&amp; echo "SUMMARY exists"
    </automated>
  </verify>
  <done>
    Fresh clone into /tmp/coverage-fresh completes bundle install and rspec without errors.
    SUMMARY.md exists with beat results table, md5 hashes, and final DEMO READY verdict.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git clone (remote→local) | Cloning from a public GitHub repo — trust boundary for remote content |
| shell commands in smoke test | Commands run against /tmp/coverage-fresh — isolated from project root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | tmp/mutant-report.txt (on demo branches) | accept | Reports are committed artifacts; the plan reads them, does not regenerate unless drift is found. If drift fix is needed, the plan documents the change in SUMMARY. |
| T-06-02 | Information Disclosure | /tmp/coverage-fresh clone | mitigate | Clone is removed with `rm -rf /tmp/coverage-fresh` at the end of Task 3. No credentials in the repo. |
| T-06-03 | Denial of Service | md5 non-determinism loop | accept | Three runs is bounded; if hashes differ the executor identifies the source rather than looping. |
| T-06-SC | Tampering | No new npm/pip/cargo installs | accept | No new package installs in this phase. Bundler runs against an existing Gemfile.lock. |
</threat_model>

<verification>
All three VAL requirements are covered by the three tasks:

- VAL-01 (full two-act demo runs end-to-end without errors): Task 1 walks all 14 beats and
  fixes drift.
- VAL-02 (every shot-list command copy-pasteable and produces documented output): Task 1
  verifies each beat's expected anchor literals.
- VAL-03 (three consecutive --replay runs produce byte-identical output): Task 2 runs the
  replay pipeline three times and md5-matches the results.

Fresh-clone smoke test (Task 3) covers the "recording machine" language in VAL-01 —
no machine-state assumptions should survive this check.
</verification>

<success_criteria>
1. All 14 beats execute without error; every expected anchor string is present in the actual output
2. Three consecutive replay pipeline runs on demo/weak-tests produce identical md5 hashes for tmp/llm-mutation-report.md
3. Fresh clone into /tmp/coverage-fresh: `bundle install` outputs `Bundle complete!` and `bundle exec rspec` outputs `28 examples, 0 failures`
4. SUMMARY.md exists with beat table, md5 hashes, smoke result, and DEMO READY verdict
5. Current branch is main when the plan completes
</success_criteria>

<output>
Create `.planning/phases/06-end-to-end-validation/06-01-SUMMARY.md` when done.
</output>
